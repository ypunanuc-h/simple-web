# PLAN — แผนส่งมอบ ems ภายใน 2 วัน

แผนนี้แปลง `SPEC.md` §7 เป็นลำดับงานที่ทำจริงได้ โดยหั่นเป็น **slice** — หนึ่ง slice คือ **หนึ่งพฤติกรรมที่รันและโชว์ได้จบในตัว** ทำเสร็จแล้วต้องมีอะไรให้เปิดดูได้เสมอ ไม่ใช่ชั้นของโค้ดที่ยังต่อไม่ติด

เอกสารนี้อยู่ใต้ `SPEC.md` และ `CLAUDE.md` ถ้าขัดกันให้ถือว่าสองไฟล์นั้นถูก แผนนี้ผิด

> **ฉบับที่ 2** — คำนวณเวลาใหม่ทั้งหมดหลังตรวจพบ 7 จุดที่ทำให้ฉบับแรกส่งไม่ทัน สรุปสิ่งที่เปลี่ยนอยู่ท้ายเอกสารหัวข้อ 8

---

## 1. วิธีใช้แผนนี้

- **หนึ่ง slice = หนึ่งการสาธิต** ทุก slice มีหัวข้อ `Demo` ที่บอกคำสั่งหรือหน้าจอที่เปิดดูได้ทันที ถ้า slice ไหนไม่มีอะไรให้เปิดดู แปลว่าหั่นผิด
- **ลำดับตายตัว** slice หลังพึ่งของจาก slice หน้า
- **ตรวจ AC ท้ายทุก slice ทันที** ไม่ใช่ไปกองตรวจรวดเดียวตอนจบ ทุก slice มีรายการ AC ของตัวเองอยู่แล้ว ใช้เป็นใบตรวจได้เลย
- **ด่านหยุด 9 ด่าน** คือจุดที่งานหยุดรอผู้ใช้ — 🛑 คือหยุดให้อ่านแผนย่อยก่อนลงมือ (4 ด่าน) และ 🧪 คือหยุดให้อ่านเทสต์ก่อน implement (5 ด่าน) ตาม `CLAUDE.md` §3 **เวลาของด่านเหล่านี้ถูกนับแยกไว้ในงบแล้ว** ไม่ได้ซ่อนอยู่ในเวลาเขียนโค้ด
- ชื่อเทสต์ทุกตัวขึ้นต้นด้วยเลข AC ตาม `CLAUDE.md` §6

---

## 2. งบเวลา

### 2.1 งานที่ commit ไว้ — ทำให้ครบคือส่งมอบได้

| slice | เวลาเขียนโค้ด | ด่านหยุด | รวม |
|---|---|---|---|
| S0 ยกระบบ + schema + โครงเทสต์ + scaffold React | 2.8 | 0.1 | **2.9** |
| S1 Seed จาก Excel | 2.5 | 0.25 | **2.75** |
| S2 รายการ/เรียง/แบ่งหน้า (API) | 2.5 | 0.25 | **2.75** |
| S3 หน้ารายการบนเว็บ | 1.0 | 0.1 | **1.1** |
| S4 ค้นหาและกรอง | 2.1 | 0.15 | **2.25** |
| S5a Employee CRUD แกนหลัก | 2.0 | 0.25 | **2.25** |
| S6 ฟอร์มสร้าง/แก้ไข + ยืนยันการลบ + toast | 2.0 | — | **2.0** |
| S8 รวมรายงานตรวจรับ | 0.5 | — | **0.5** |
| **รวม** | **15.4** | **1.1** | **16.5** |

| | ชม. |
|---|---|
| เวลาที่มี | **16.0** |
| งานที่ commit | **16.5** |
| **buffer เหลือ** | **-0.5** |

**เกินงบไป 0.5 ชม.** — สาเหตุคือ S6 ขยายจาก "create + confirm-delete" เป็น "create/update/delete ครบผ่าน modal เดียวกัน + toast" ตามที่ผู้ใช้ขอเพิ่มระหว่างทาง ดึงงาน "แก้ไขผ่านฟอร์ม" จากรายการเพิ่มหัวข้อ 6.1 เข้ามาเป็นงาน commit (ดูหัวข้อ 6.1 ที่ตัดรายการนั้นออกไปแล้ว) ณ จุดนี้ไม่มีเวลาเหลือให้พลาดอีกเลยก่อนถึง S8 — เดิม buffer 12 นาทีคือความจริงที่ต้องรับไว้อยู่แล้ว ตอนนี้ติดลบ จึงต้องดู checkpoint หัวข้อ 2.3 และรายการสละหัวข้อ 6.2 อย่างเคร่งครัดกว่าเดิมถ้ามีงานเกินแผนอีก (หมายเหตุ — ตัวเลข "ชม." ในเอกสารนี้เป็นหน่วยงบประมาณเวลาตามแผนเดิม ไม่ใช่นาฬิกาจริงที่เดินไปพร้อมบทสนทนานี้ ซึ่งข้ามหลายรอบการทำงานมาแล้ว)

งานที่ commit นี้ครอบคลุม **P0 ครบทั้ง 39 ข้อ** และข้อบังคับของ `CLAUDE.md` (เทสต์ TZ ที่ไม่ใช่ UTC, เทสต์กับ Postgres จริง, TDD ในส่วนที่เป็นตรรกะจริง) **ไม่มีอะไรเกินกว่านั้น** ทุกอย่างที่เป็น P1 ถูกย้ายไปหัวข้อ 6.1 หมดแล้ว

### 2.2 การแบ่งวัน

| วัน | slice | ชม. | สภาพเมื่อจบวัน |
|---|---|---|---|
| **วันที่ 1** | S0, S1, S2 | 8.4 | ข้อมูลจริงจากไฟล์อยู่ในฐานข้อมูลถูกต้อง และเปิด URL ดู JSON ที่เรียง/แบ่งหน้าได้ |
| **วันที่ 2** | S3, S4, S5a, S6, S8 | 8.1 | ค้นหา กรอง สร้าง/แก้ไข/ลบ ได้จากหน้าเว็บ พร้อม toast และรายงานตรวจรับ |

วันที่ 1 เกิน 12 นาที วันที่ 2 เดิมเหลือ 24 นาที แต่ S6 ที่ขยายขอบเขต (+0.7) ทำให้วันที่ 2 เกินไปแล้วราว 18 นาที — **ทั้งสองวันรวมกันติดลบ buffer 0.5 ชม. ตามหัวข้อ 2.1** ถ้ามีงานเกินแผนอีกให้เปิดหัวข้อ 6.2 ทันที ไม่ใช่รอดูสถานการณ์

### 2.3 Checkpoint — เช็กที่นาฬิกา ไม่ใช่เช็กที่ความรู้สึก

| เวลาสะสม | ต้องจบอะไรแล้ว |
|---|---|
| 2.9 ชม. | S0 — ระบบขึ้น ตารางตรงตาม spec เทสต์รันได้ |
| 5.7 ชม. | S1 — seed แล้วข้อมูล 5 แถวถูกต้อง |
| 8.4 ชม. (จบวันที่ 1) | S2 — list API ครบ |
| 9.5 ชม. | S3 — หน้ารายการเปิดดูได้ |
| 11.7 ชม. | S4 — ค้นหาและกรองใช้ได้จากหน้าเว็บ |
| 14.0 ชม. | S5a — CRUD ผ่าน API ครบ |
| 15.3 ชม. | S6 — สร้างและลบจากหน้าเว็บได้ |
| **T-0.5 (15.5 ชม.)** | **เส้นตายแข็ง — เริ่ม S8 ไม่ว่างานค้างอยู่ตรงไหน** |

**กฎเดียวที่ต้องทำตาม** — เลย checkpoint ไหนเกิน 45 นาที ให้หยิบรายการสละหัวข้อ 6.2 ข้อบนสุดมาใช้ทันที ห้ามแก้ด้วยการเร่งมือ เพราะจุดที่เร่งแล้วพังคือ import กับ decimal ซึ่งพังแบบเงียบ ๆ

---

## 3. ภาพรวม slice

| # | slice | ส่งมอบให้เห็นอะไร | P0 | ชม. |
|---|---|---|---|---|
| S0 🛑 | ยกระบบ + schema + โครงเทสต์ | `docker compose up` ขึ้นครบ ตารางตรง spec `npm test` รันได้ | — | 2.9 |
| S1 🛑🧪 | Seed จาก Excel | รันคำสั่งเดียวแล้วได้ข้อมูล 5 แถวที่ถูกต้อง | 5 | 2.75 |
| S2 🛑🧪 | รายการ/เรียง/แบ่งหน้า (API) | เปิด URL แล้วเห็น JSON ที่เรียงและแบ่งหน้าถูก | 8 | 2.75 |
| S3 🧪 | หน้ารายการบนเว็บ | ตารางจริงในเบราว์เซอร์ | 1 | 1.1 |
| S4 🧪 | ค้นหาและกรอง | พิมพ์ค้นหาแล้วตารางเปลี่ยนตาม | 10 | 2.25 |
| S5a 🛑🧪 | Employee CRUD แกนหลัก | สร้าง/แก้/ลบผ่าน API และ error รูปแบบเดียวกันหมด | 12 | 2.25 |
| S6 | ฟอร์มสร้าง/แก้ไข + ยืนยันการลบ + toast | สร้าง แก้ไข และลบพนักงานจากหน้าเว็บครบ พร้อม toast แจ้งผล | 3 | 2.0 |
| S8 | รวมรายงานตรวจรับ | รายงาน P0 39 ข้อ + README | — | 0.5 |
| S7 (นอกงบเดิม) | Department CRUD ฝั่งเขียน + หน้าจัดการแผนก | สร้าง/แก้ไข/ลบแผนกจากหน้าเว็บ, dropdown พนักงานเห็นแผนกใหม่ทันที | — (P1 ล้วน) | ~1.5+ |

---

## 4. รายละเอียดแต่ละ slice

### S0 — ยกระบบ + schema + โครงเทสต์ 🛑 · 2.9 ชม.

**ส่งมอบ**

| ของ | ชม. |
|---|---|
| `docker-compose.yml` (db/api/web ตรึง tag ระดับ patch, healthcheck, named volume, **mount `example_data/` แบบ read-only เข้า api**) + `.env.example` | 0.5 |
| NestJS บูตได้ + global `ValidationPipe` (`whitelist`, `forbidNonWhitelisted`, `transform`) + exception filter เปล่า | 0.4 |
| entity สองตัว + data source `synchronize: false` | 0.3 |
| migration แรก — สองตาราง + index ครบ 5 ตัวตาม §6.1 | 0.5 |
| **โครงเทสต์** — ฐานข้อมูลเทสต์แยกคนละตัวจาก dev, ตัวรัน migration ก่อน e2e, ตัวล้างข้อมูลระหว่างเทสต์, `TZ=UTC` เป็นค่าเริ่มต้น | 0.7 |
| **`Clock` provider ที่ inject ได้** เพื่อหยุดเวลาในเทสต์ | 0.2 |
| scaffold React ขั้นต่ำ + Vite proxy ให้ compose ขึ้นครบสามตัว (ย้ายมาจาก S3) | 0.2 |

**Demo**

```
docker compose up -d
curl http://localhost:3000/api/health            # {"status":"ok"}
docker compose exec db psql -U ems -d ems -c "\d employees"
npm test                                          # รันได้ ต่อฐานข้อมูลเทสต์ติด ยังไม่มีเทสต์จริง
```

ยืนยันด้วยตาจาก `\d` ว่า `salary` เป็น `numeric(12,2)`, `join_date` เป็น `date`, `updated_at` เป็น `timestamptz`, ทุกคอลัมน์ `NOT NULL`, `id` เป็น identity แบบ `BY DEFAULT`

**AC** — P0: ไม่มี · P1: **AC-N05, N09, N03**

**สองอย่างที่เพิ่มเข้ามาในฉบับนี้และเหตุผล**

- **ฐานข้อมูลเทสต์ต้องแยกจาก dev ตั้งแต่วันแรก** ถ้า e2e รันทับฐานข้อมูล dev การสร้างพนักงานในเทสต์จะดัน identity sequence ไปเรื่อย ๆ แล้ว demo ของ S5a ที่ประกาศว่าจะได้ `id = 106` จะพังต่อหน้าคนดู และ AC-I07 พิสูจน์ไม่ได้อีกเลย
- **`Clock` ต้องมีก่อนเขียนบรรทัดแรกของ D1/D2** เพราะ `CLAUDE.md` §6 ห้าม assert กับ `new Date()` ตรง ๆ ถ้าปล่อยให้ service เรียกเวลาปัจจุบันเองไปก่อน จะต้องรื้อทุกจุดที่ stamp `updated_at` ตอน S5a ซึ่งแพงกว่าทำตอนนี้หลายเท่า

**ต้องตัดสินใน slice นี้** — FE คุย API ผ่าน Vite proxy หรือเปิด CORS ที่ Nest เลือกอย่างใดอย่างหนึ่งแล้วจบ อย่าไปเจอตอน S3

**จงใจไม่ทำ** — ยังไม่มี route ใดนอกจาก health, หน้าเว็บเป็นหน้าเปล่าที่ยังไม่เรียก API (ตาราง ตัวเรียก API และตัวจัดรูปแบบอยู่ที่ S3)

---

### S1 — Seed จาก Excel 🛑🧪 · 2.75 ชม.

**ส่งมอบ**

| ของ | ชม. |
|---|---|
| ตัวอ่าน `.xlsx` ด้วย `exceljs` อ้าง cell reference | 0.4 |
| แปลง serial → `YYYY-MM-DD` ด้วยเลขจำนวนเต็มจาก `1899-12-30` + reject serial < 61 | 0.3 |
| normalize Status/Department | 0.2 |
| mapper + นโยบายแถวเสีย + log แถวที่ข้าม | 0.4 |
| upsert ตาม D1 (`IS DISTINCT FROM`) | 0.6 |
| `setval` หลัง import | 0.2 |
| `npm run seed` + รายงานสรุป เพิ่ม/แก้/ไม่เปลี่ยน/ข้าม | 0.4 |

**Demo**

```
docker compose run --rm api npm run seed
# inserted 5, updated 0, unchanged 0, skipped 0

docker compose exec db psql -U ems -d ems \
  -c "SELECT id, name, salary, join_date, is_active, updated_at FROM employees ORDER BY id"

docker compose exec db psql -U ems -d ems \
  -c "SELECT last_value FROM pg_get_serial_sequence('employees','id')"   # 105
```

ชี้กับดักสามข้อ — `101` ได้ `65000.00` ไม่ใช่ `65`, `join_date` เป็น `2023-01-15` พอดี, `104` ได้ `is_active = false` ทั้งที่ไฟล์เขียน `"In Active"` แล้วรันซ้ำให้ดูว่ารอบสองรายงาน `updated 0`

**บรรทัด `last_value` มีไว้เพื่อพิสูจน์ AC-I07 ให้จบในสไลซ์นี้** เพราะการสร้างพนักงานคนที่ `106` จริง ๆ ต้องรอ `POST` จาก S5a ถ้าไม่เช็ก sequence ตรงนี้ สไลซ์นี้จะโชว์ P0 ได้แค่ 4 จาก 5 ข้อ

**AC** — P0: **AC-I01, I02, I03, I05, I07** · P1: **AC-I04** (TZ `Asia/Bangkok` และ `America/Los_Angeles` ได้วันเดียวกัน — `CLAUDE.md` §6 บังคับ ห้ามตัด), **AC-I06, I13, I15, I16, I19, I20**

**TDD — หนึ่งด่าน 🧪 ไม่ใช่สามด่าน**

เขียนเทสต์รวดเดียวทั้ง unit (serial, normalize, mapper) และ integration (upsert D1, setval บน Postgres จริง) แล้วหยุดให้อ่านครั้งเดียว จากนั้นค่อย implement ทั้งก้อน — ฉบับแรกแยกเป็นสามรอบ ซึ่งแปลว่าหยุดรอสามครั้งในสไลซ์เดียว ไม่คุ้มกับเวลาที่มี

**จงใจไม่ทำ** — AC-I08 ถึง I18 ที่ต้องแก้ไฟล์ต้นฉบับหลายรูปแบบ อยู่ในรายการเพิ่มหัวข้อ 6.1

> **ทางหนีถ้าเลย checkpoint 5.7 ชม. เกิน 45 นาที** — ดูหัวข้อ 6.2 ข้อ 2 ตัดสินตอนนั้นเลย ไม่ใช่ปล่อยยาว

---

### S2 — รายการ/เรียง/แบ่งหน้า (API) 🛑🧪 · 2.75 ชม.

**ส่งมอบ** — `GET /api/employees` พร้อม `sort`/`order`/`page`/`page_size`, ฝัง `department` object ในทุกแถวด้วย join เดียว, `meta` ครบสี่ค่าโดยนับ `total` ด้วย `COUNT` ใน SQL, ต่อท้าย `id ASC` ทุกการเรียงตาม D11, `sort` ผ่าน whitelist, และ `GET /api/employees/{id}`

**Demo** — เปิดในเบราว์เซอร์ได้เลยเพราะเป็น `GET` ล้วน

```
http://localhost:3000/api/employees
http://localhost:3000/api/employees?sort=salary&order=desc
http://localhost:3000/api/employees?page_size=2&page=2
http://localhost:3000/api/employees?sort=id;DROP%20TABLE%20employees   → 400 และตารางยังอยู่
http://localhost:3000/api/employees/999                                 → 404
```

**AC** — P0: **AC-L01, L14, L16, L21, L25, E01, E02, N04** · P1: **AC-L17, L18, L19, N01, N16**

**สอง fixture ที่ต้องสร้างในเทสต์เอง อย่าเผลอคิดว่ามีอยู่แล้ว**

- **AC-L16 ต้องมีพนักงานเงินเดือนเท่ากันอย่างน้อยสองคน** ข้อมูลที่ seed มาทั้ง 5 แถวเงินเดือนไม่ซ้ำกันเลยสักคู่ (65000 / 58000 / 45000 / 72000 / 50000) ถ้าไม่สร้างเพิ่ม เทสต์ความเสถียรของการเรียงจะผ่านโดยไม่ได้พิสูจน์อะไร
- **AC-N04 ต้องมี 25 แถว** สร้างในเทสต์ ไม่ใช่ในฐานข้อมูล dev

**TDD — หนึ่งด่าน 🧪** เทสต์ service ว่า sort นอก whitelist ถูกปฏิเสธและ order ที่ส่งลง repository มี `id ASC` ต่อท้ายเสมอ รวมกับ e2e ของ AC ข้างบน แล้วหยุดให้อ่านครั้งเดียว

**จงใจไม่ทำ** — ยังไม่มี `q` และ filter (อยู่ S4) และ **ไม่มี `sort=department`** (AC-L15 อยู่ในรายการเพิ่ม)

---

### S3 — หน้ารายการบนเว็บ 🧪 · 1.3 ชม.

**ส่งมอบ** — `lib/api-client.ts` (0.2), ตารางพนักงาน + ปุ่ม "เพิ่มพนักงาน" ที่ยังไม่ทำงาน (0.6), ตัวจัดรูปแบบเงินเดือน `#,##0.00` ที่ชั้น presentation เท่านั้น ค่าใน state ยังเป็น string (0.2)

**Demo** — เปิด `http://localhost:5173` ให้ดูคอลัมน์ Salary ที่แสดง `65,000.00` และคอลัมน์ Department ที่แสดงชื่อแผนก ไม่ใช่ตัวเลข id แล้วลองเปลี่ยน timezone ของเครื่องให้ดูว่า `join_date` ไม่เลื่อนวัน

**AC** — P0: **AC-UI01** · P1: **AC-UI10** (สถานะว่าง), **AC-N08**, **AC-N14** (Chromium)

**หัวคอลัมน์ในสไลซ์นี้คลิกเรียงไม่ได้** เพราะการเรียงกับแบ่งหน้าบน UI ถูกย้ายไปรายการเพิ่มหัวข้อ 6.1 ข้อ 2 **และถ้าทำทีหลัง ห้ามใส่หัวคอลัมน์ Department ให้คลิกได้** ตราบใดที่ยังไม่ทำ AC-L15 เพราะ API จะตอบ `400` กลางการสาธิต

**TDD — หนึ่งด่าน 🧪** เฉพาะ `format.ts` (string → `#,##0.00` โดยไม่ผ่าน float) ส่วน layout เขียนตรงได้

---

### S4 — ค้นหาและกรอง 🧪 · 2.25 ชม.

**ส่งมอบ** — `q`, `department_id`, `is_active`, `join_date_from/to`, `salary_min/max` รวมกันแบบ AND ทั้งฝั่ง API (1.1) e2e (0.6) และแถบกรองบนหน้ารายการที่ประกอบ query string จากฟอร์ม (0.4)

**Demo** — บนหน้าเว็บ พิมพ์ `jo` เลือกแผนก Engineering ติ๊ก Active ใส่ช่วงเงินเดือน 50000–65000 แล้วให้ดูว่าเหลือแถวเดียว

**AC** — P0: **AC-L02, L03, L05, L07, L08, L09, L10, L11, L12, L13** · P1: **AC-L04, L06, L20, L24**

**TDD — หนึ่งด่าน 🧪** รวมเทสต์ service (trim `q` แล้วว่างถือว่าไม่ได้ส่ง, รวมเงื่อนไข AND), e2e ของ AC-L02–L13 และ unit ของ `build-query.ts` ฝั่ง React ไว้ในเทิร์นเดียว เพราะทั้งหมดเป็นตรรกะการประกอบเงื่อนไขชุดเดียวกัน

**จงใจไม่ทำ** — การตรวจข้ามฟิลด์ `salary_min > salary_max` และ `join_date_from > join_date_to` (AC-L22, L23)

---

### S5a — Employee CRUD แกนหลัก 🛑🧪 · 2.25 ชม.

**ส่งมอบ**

| ของ | ชม. |
|---|---|
| DTO ของ `POST`/`PUT` + กฎ §5.1 เฉพาะที่เป็น P0 (`name` required/length, `salary` format, `department_id` มีจริง) | 0.3 |
| ตัวเทียบว่าค่าเปลี่ยนจริงหรือไม่ ตาม D2 ใช้ `Clock` จาก S0 | 0.3 |
| service + ตรวจ department มีจริงก่อนเขียน + domain error | 0.4 |
| exception filter ตัวเดียวแปลงทุก error เป็นรูปแบบ §4.1 รวม error ของ class-validator เป็น `details[]` | 0.4 |
| controller + `Location` header + `GET /api/departments` | 0.2 |
| e2e | 0.4 |

**Demo**

```
curl -i -X POST localhost:3000/api/employees -H "Content-Type: application/json" \
  -d '{"name":"Dana Lee","department_id":1,"salary":"61000.00","join_date":"2026-09-01"}'
# 201 + Location + id = 106

curl -X POST ... -d '{"salary":"65,000.00", ...}'      # 400 rule format ไม่ใช่ 65
curl -X POST ... -d '{"department_id":9999, ...}'      # 400 rule not_found ไม่ใช่ 500
curl -X PUT  ... (ส่งค่าเดิมทุกช่อง)                    # 200 และ updated_at ไม่ขยับ
curl -i -X DELETE localhost:3000/api/employees/103     # 204 แล้ว GET ได้ 404 total เหลือ 4
```

**AC** — P0: **AC-E03, E04, E09, E11, E13, V01, V04, V05, V19, U01, U03, D01** · P1: **AC-E10, E12, U02, U05, U06, D02, N06, N07, N10**

**นี่คือสไลซ์ที่แบก P0 มากที่สุด 12 จาก 39 ข้อ** ฉบับแรกรวมกฎ validation ปลีกย่อยทั้งหมดไว้ด้วยเป็นก้อน 3.5 ชม. ที่ตัดครึ่งไม่ได้ ฉบับนี้แยกกฎที่เป็น P1 (V02, V03, V06–V18) ออกไปเป็น **S5b** ในรายการเพิ่มหัวข้อ 6.1 ข้อ 3 ทำให้สไลซ์นี้เหลือแค่สิ่งที่บล็อกการส่งมอบจริง ๆ และมีเส้นตัดที่ชัดเจน

**TDD — หนึ่งด่าน 🧪** รวมเทสต์ตัวเทียบค่าเปลี่ยน (D2), validator ของ `salary`, การตรวจ department มีจริง และ e2e ของ CRUD กับรูปแบบ error ไว้ในเทิร์นเดียว **ต้องมีเทสต์ที่ยืนยันว่า driver คืน `decimal` เป็น string** (AC-N06, N07) ตาม `CLAUDE.md` ห้ามเชื่อเอกสารแล้วข้ามไป ถ้าพบว่าไม่ตรงต้องหยุดถามก่อนแก้

**จงใจไม่ทำ** — `POST`/`PUT`/`DELETE /api/departments` (P1 ทั้งก้อน อยู่หัวข้อ 6.1 ข้อ 10)

---

### S6 — ฟอร์มสร้าง/แก้ไข + ยืนยันการลบ + toast · 2.0 ชม.

> **ขยายขอบเขตระหว่างทาง** — ผู้ใช้ขอให้ทำ create/update/delete ครบจากหน้าเว็บ (ไม่ใช่แค่ create+delete ตามแผนเดิม) และเพิ่ม toast แจ้งผลสำเร็จ/ผิดพลาด ดึงชิ้นแรกของรายการเพิ่มหัวข้อ 6.1 (แก้ไขผ่านฟอร์ม) เข้ามาเป็นงาน commit พร้อม AC-UI05/UI06 ที่ตามมาด้วยธรรมชาติ เวลาที่เพิ่มขึ้น (+0.7 จาก 1.3) มาจาก `EmployeeForm` ที่ใช้ร่วมสร้าง/แก้ไข, `Modal` overlay, `Toast`, และ `employee-payload.ts` ที่ต้องมีด่าน 🧪 เพราะมีตรรกะแปลงค่า (D7 salary ต้องไม่ผ่าน number, D10 ห้ามมี id ปน)

**ส่งมอบ** — ฟอร์มเดียวใช้ร่วมกันสร้าง/แก้ไขผ่าน modal (`mode: 'create' | 'edit'`), ซ่อนช่อง ID/Last Updated ตอนสร้าง (AC-UI05) และล็อกเป็น read-only ตอนแก้ไข (AC-UI06), dropdown แผนกจาก `GET /api/departments` (AC-UI02), checkbox Status (AC-UI04), ปุ่มแก้ไข/ลบต่อแถวในตาราง, กล่องยืนยันก่อนลบผ่าน modal เดียวกัน (AC-UI07), toast แจ้งผลสำเร็จ/ผิดพลาดของทั้งสร้าง/แก้ไข/ลบ (แทนที่แผนเดิมที่จะโชว์ error เป็นข้อความก้อนเดียวเหนือฟอร์ม — toast ครอบคลุมความต้องการเดิมได้โดยไม่ต้องมีสองจุดแสดง error ซ้ำกัน)

**Demo** — จากหน้าเว็บล้วน ๆ กดเพิ่มพนักงาน กรอกฟอร์ม บันทึก เห็น toast สำเร็จและแถวใหม่ในตาราง กดแก้ไขแถวใดก็ได้ เห็นฟอร์มเดิมพร้อมค่าเดิมและ ID/Last Updated เป็น read-only บันทึกแล้วเห็นค่าที่เปลี่ยน แล้วกดลบ ให้ดูว่ามีกล่องยืนยัน กดยกเลิกแล้วข้อมูลยังอยู่ กดยืนยันแล้วหายจากตารางพร้อม toast

**AC** — P0: **AC-UI02, UI04, UI07** · P1 ที่ได้เพิ่ม: **AC-UI05, UI06**

**จงใจยังไม่ทำ (ตอน S6)** — AC-UI08 (error รายช่อง ยังเป็น toast ก้อนเดียว), AC-UI03 (ขึ้นกับหน้าจัดการแผนก — S7 ทำเสร็จภายหลังแล้ว ดูหัวข้อ S7 ด้านล่าง)

**TDD — หนึ่งด่าน 🧪** เฉพาะ `employee-payload.ts` (แปลงค่าฟอร์ม→request body) ส่วน `Modal`/`Toast`/`EmployeeForm`/การเชื่อม `EmployeeListPage` เป็น layout กับ wiring ที่ไม่มีเงื่อนไขจึงเขียนตรงได้ตามบรรทัดฐานเดียวกับ S3

---

### S7 — Department CRUD ฝั่งเขียน + หน้าจัดการแผนก · ทำเสร็จหลัง S8 (นอกงบเดิมทั้งหมด)

**ส่งมอบ** — `POST`/`PUT /:id`/`DELETE /:id` `/api/departments` ครบ (ไม่มี `Location` header ตาม POST เพราะ SPEC §4.5 ไม่ได้ระบุไว้ต่างจาก employees), domain error ใหม่สองตัว (`DuplicateNameError`→409 `DUPLICATE_NAME`, `DepartmentInUseError`→409 `DEPARTMENT_IN_USE`), ย้าย `normalizeForComparison` (D6) จาก `import/` ไป `common/normalize.ts` ให้สองฟีเจอร์ใช้กฎเดียวกันจริง, หน้าจัดการแผนกบนเว็บ (`DepartmentsPage.tsx`) พร้อมปุ่มลบที่ disable ล่วงหน้าเมื่อ `employee_count > 0` (ใช้ข้อมูลที่ `GET /api/departments` ส่งมาอยู่แล้วตามเจตนาที่ SPEC §4.5 บอกไว้), ย้าย `Modal`/`Toast` จาก `features/employees/` ไป `web/src/components/` เพราะไม่ใช่ของ employee โดยเฉพาะ, สลับหน้า Employees ↔ Departments ด้วย tab state ใน `App.tsx` **ไม่ใช้ router** (ตัดสินใจเดียวกับ S6)

**Demo**
```
curl -X POST localhost:3000/api/departments -d '{"name":"Finance"}'        # 201
curl -X POST localhost:3000/api/departments -d '{"name":"finance"}'        # 409 DUPLICATE_NAME
curl -X DELETE localhost:3000/api/departments/1                            # 409 DEPARTMENT_IN_USE (Engineering มีคน)
```
หน้าเว็บ — แท็บ "แผนก" เพิ่ม/แก้ไข/ลบผ่าน modal เดียวกับพนักงาน แล้วสลับกลับแท็บ "พนักงาน" ดู dropdown มีแผนกใหม่ทันที (AC-UI03)

**AC** — P1 ทั้งหมด: **AC-D03–D14 (12 ข้อ, ปิดหมวด 7.3 ครบ 100%), AC-UI03**

**บั๊กที่เจอระหว่างทำ** — e2e fixture ของทั้ง `employees.e2e-spec.ts` และ `departments.e2e-spec.ts` seed แผนกด้วย id ตายตัว (1-4) โดยไม่เคยรีเซ็ต identity sequence หลังจากนั้น ทำให้ `POST /api/departments` ตัวแรกชนกับแถว id=1 ที่มีอยู่แล้ว (`nextval()` ค้างที่ 1 ตั้งแต่ `RESTART IDENTITY`) แก้ด้วย `resetDepartmentIdentitySequence` ใน `test/setup-db.ts` มิเรอร์ `resetEmployeeIdentitySequence` ที่มีอยู่แล้วฝั่ง employees (D10) — เป็นบั๊กระดับ test fixture ไม่ใช่โค้ด production เพราะ production ไม่เคย insert แผนกด้วย id ที่ระบุเองเลย

**TDD — หนึ่งด่าน 🧪** เฉพาะ `duplicate-name.ts` (หา department ที่ชื่อชนกันตาม D6, `excludeId` กันชนตัวเองตอน PUT) และเคส e2e ใหม่ 12 เคสคุม AC-D03–D14 — service CRUD (`create`/`update`/`delete`) คุมด้วย e2e ตามบรรทัดฐานเดียวกับ employees ไม่มี unit test แยก

---

### S8 — รวมรายงานตรวจรับ · 0.5 ชม.

**ส่งมอบ** — รันทั้ง suite หนึ่งรอบ, รวมผลตรวจ P0 ที่เก็บไว้ท้ายแต่ละ slice เป็นรายงานเดียว, บันทึกรายการ P1 ที่ไม่ได้ทำพร้อมเลข AC ตาม §7.10 ข้อ 3, `README.md` วิธียกระบบขึ้นและวิธี seed

**Demo** — เปิดรายงานอ่านทีละข้อ พร้อม output ของ `npm test` ที่ชื่อเทสต์ขึ้นต้นด้วยเลข AC

**เริ่มที่ T-0.5 เสมอ ไม่ว่างานค้างอยู่ตรงไหน** ฉบับแรกให้เวลา 1.0 ชม. และวางไว้ท้ายสุดโดยหวังว่าจะมีเวลาเหลือ ซึ่งเป็นสิ่งแรกที่จะโดนกิน ฉบับนี้ย้ายการตรวจ AC ไปทำท้ายแต่ละ slice แล้ว เหลือที่นี่แค่การรวมรายงาน จึงเหลือครึ่งชั่วโมงพอ

---

## 5. ตารางตรวจ P0 ครบ 39 ข้อ

| slice | AC ที่เป็น P0 | จำนวน |
|---|---|---|
| S1 | I01, I02, I03, I05, I07 | 5 |
| S2 | L01, L14, L16, L21, L25, E01, E02, N04 | 8 |
| S3 | UI01 | 1 |
| S4 | L02, L03, L05, L07, L08, L09, L10, L11, L12, L13 | 10 |
| S5a | E03, E04, E09, E11, E13, V01, V04, V05, V19, U01, U03, D01 | 12 |
| S6 | UI02, UI04, UI07 | 3 |
| **รวม** | | **39** |

ตรงกับ `SPEC.md` §7.0.1 ทุกข้อ **ไม่มี P0 ข้อใดอยู่ในรายการเพิ่มหัวข้อ 6.1 ที่อาจไม่ได้ทำ** และไม่มีข้อใดถูกนับซ้ำสอง slice

กับดักสี่ข้อที่ §7.10 บังคับเป็นพิเศษ — I05, I03, I02 พิสูจน์ที่ S1, V05 ที่ S5a, ส่วน I07 พิสูจน์สองชั้น คือ `last_value = 105` ตอนจบ S1 และ `id = 106` จริงตอน S5a

---

## 6. เวลาเหลือทำอะไร เวลาขาดสละอะไร

### 6.1 รายการเพิ่ม — หยิบได้เมื่อถึง checkpoint ก่อนเวลาเท่านั้น

เรียงตามความคุ้มค่าต่อชั่วโมง หยิบจากบนลงล่าง **ห้ามหยิบเพราะรู้สึกว่ามีเวลา ต้องดูนาฬิกาที่ checkpoint จริง**

> **แก้ไขระหว่างทำ S8** — ตอนรวบรวมรายงานตรวจรับ ไล่ grep เทียบทุก AC กับเทสต์จริงในโค้ดแล้วพบว่ารายการด้านล่างนี้ค้างมาตั้งแต่ก่อน S6 ไม่ตรงกับของจริงสองจุด: **S5b (แถว 2 เดิม)** ถูกดูดเข้าไปทำใน S5a ไปแล้วตั้งแต่ตอนเลือก "ทำ §5.1 เต็ม P0+P1" (ดู `PROMPTS.md` #41–43) — V02, V03, V06–V18 มีเทสต์ e2e ครบทุกตัว ไม่ใช่งานค้าง และ **เทสต์กรณีขอบของ import (แถว 3 เดิม)** ก็ถูกเขียนเกือบครบระหว่าง S1/S4 แล้วเช่นกัน เหลือจริงแค่ I10 กับ I18 ไม่ใช่ I08–I18 ทั้งก้อน ตารางด้านล่างแก้ให้ตรงกับของจริงแล้ว

| ลำดับ | เพิ่มอะไร | ชม. | ได้ AC |
|---|---|---|---|
| 1 | **เรียงและแบ่งหน้าบน UI** (S3) | 0.7 | UI09 (คง filter ไว้ข้ามหน้า), เรียงคอลัมน์คลิกได้ |
| 2 | เทสต์กรณีขอบของ import ที่เหลือจริง | 0.2 | I10, I18 |
| 3 | ตรวจข้ามฟิลด์ของช่วงค่าใน query | 0.4 | L22, L23 |
| 4 | แสดง error รายช่องบนฟอร์ม (แทนที่ toast ก้อนเดียว) | 0.4 | UI08 |
| 5 | `sort=department` | 0.3 | L15 |
| 6 | ตรวจ `EXPLAIN` และเทสต์ log ของ 500 (N16 verified แล้วด้วยการอ่านโค้ดใน S2 close-out) | 0.3 | N11, N15 |
| 7 | component test infra (jsdom + React Testing Library) + เทสต์อัตโนมัติของ AC-UI02/04/05/06/07/09/10 | 0.6 | ปิดช่องโหว่ P0 ที่ระบุใน `ACCEPTANCE.md` |
| | **รวม** | **2.9** | |

รายการเดิมข้อ 1 (แก้ไขพนักงานผ่านฟอร์ม) และข้อ 7 เดิม (ซ่อน/ล็อก ID และ Last Updated) ถูกดึงเข้าไปเป็นงาน commit ใน S6 แล้ว **ข้อ 8 เดิม (S7 — Department CRUD ฝั่งเขียน + หน้าจัดการแผนก) ทำเสร็จและ commit แล้วเช่นกัน** ดูหัวข้อ 4a ด้านล่าง — ไม่ใช่รายการเพิ่มอีกต่อไปทั้งสามข้อ

งานที่ commit 16.5 + S7 ที่ทำเสร็จเพิ่ม (ประเมินไว้ 1.5 แต่ของจริงรวมการแก้บั๊ก sequence ของ test fixture ด้วย) + รายการเพิ่มที่เหลือ 2.9 = ขนาดจริงของงานที่เหลือทั้งหมดตาม `SPEC.md` ลดลงต่อเนื่องจาก 20.9 เดิม เพราะ S7 ย้ายจากก้อน "เพิ่ม" ไปก้อน "ทำเสร็จแล้ว"

**ไม่เคยมีข้อไหนในรายการนี้อยู่ในงบ 16 ชม. ตั้งแต่ต้น** — ฉบับแรกเคยนับ S7 เป็น "เวลาที่ตัดแล้วได้คืน" ทั้งที่ไม่เคยถูกนับเข้าไป ซึ่งเป็นข้อผิดพลาดที่ร้ายที่สุดของฉบับแรก (ดูหัวข้อ 8) ตอนนี้ S7 ทำเสร็จแล้วนอกงบเดิมทั้งหมดตามที่บันทึกไว้แต่แรก

### 6.2 รายการสละ — เมื่อเลย checkpoint เกิน 45 นาที

เมื่องานที่ commit เหลือแต่ P0 ล้วนแล้ว การตามหลังแปลว่า **ต้องสละ P0** ไม่มีทางอื่น เรียงตามความเสียหายน้อยที่สุดก่อน

| ลำดับ | สละอะไร | คืน | เสีย P0 | ผลที่ตามมา |
|---|---|---|---|---|
| 1 | **S6 เหลือแค่ปุ่มลบ + กล่องยืนยัน** ตัดฟอร์มสร้างออก สาธิตการสร้างด้วย `curl` แทน | 0.9 | UI02, UI04 (2 ข้อ) | หน้าเว็บยังแสดงรายการ ค้นหา กรอง และลบได้ ซึ่งเป็นสามฟีเจอร์หลักตาม §7.0.1 ขาดเฉพาะทางเข้าของการสร้าง |
| 2 | **S1 เปลี่ยนเป็น SQL fixture 5 แถว** ตัด import จริงทิ้ง | 2.2 | I01, I02, I03, I05, I07 (5 ข้อ) | คืนเวลามากที่สุด แต่ **เสียกับดักทั้งสี่ข้อที่ §7.10 บังคับ** ใช้เป็นทางเลือกสุดท้ายเท่านั้น และต้องบันทึกไว้ให้ชัด |
| 3 | **S3 ตัดการจัดรูปแบบเงินเดือน** แสดงค่าดิบจาก API | 0.2 | UI01 (1 ข้อ) | คืนเวลาน้อยมากเทียบกับ P0 ที่เสีย อยู่ท้ายสุดด้วยเหตุผลนี้ |

### 6.3 ห้ามตัดไม่ว่าอย่างไร

- **โครงเทสต์กับฐานข้อมูลเทสต์แยก** — ตัดแล้วเทสต์ที่เหลือทั้งหมดเชื่อถือไม่ได้ และ demo `id = 106` พัง
- **migration และ `synchronize: false`** — ปล่อยให้ ORM ปรับ schema เอง ฐานข้อมูลจะเบี่ยงจาก `SPEC.md` §2 เงียบ ๆ
- **TDD ในส่วนที่เป็นตรรกะจริง** — กับดักของโปรเจกต์นี้อยู่ที่ timezone กับทศนิยม ซึ่งพังแบบไม่มี error การข้ามเทสต์แค่ย้ายเวลาไปอยู่ตอนไล่บั๊ก
- **เทสต์ที่แตะฐานข้อมูลต้องรันกับ Postgres จริง** ตาม `CLAUDE.md` §6
- **AC-I04** — เทสต์ TZ ที่ไม่ใช่ UTC เป็นข้อบังคับถึงจะเป็น P1
- **S8** — ไม่มีรายงาน ก็ไม่มีหลักฐานว่าผ่าน
- **การต่อท้าย `PROMPTS.md`** ทุกครั้ง

---

## 7. การตัดสินใจและความเสี่ยง

### 7.1 ตัวอ่าน `.xlsx` — ตัดสินแล้ว

**ใช้ `exceljs`** ผู้ใช้เลือกเมื่อ 2026-09-14 ด้วยเหตุผลว่าติดตั้งง่ายกว่า เป็น dependency เดียวที่เพิ่มจากรายการ stack ใน `CLAUDE.md` §2

`SPEC.md` §5.4 ห้ามแปลงเป็น CSV คั่นกลาง และ `ANALYSIS.md` ข้อค้นพบที่ 5 บอกว่าเซลล์ว่างหายไปทั้ง element ดังนั้นต้องอ่านด้วย `worksheet.getCell('B2')` แบบอ้าง cell reference **ห้ามวนอ่านจาก `row.values`** เพราะ array นั้นเลื่อนตำแหน่งเมื่อมีเซลล์ว่าง ซึ่งเป็นบั๊กคอลัมน์เลื่อนที่ AC-I14 จับอยู่พอดี

### 7.2 กฎ seed แผนก — ตัดสินแล้ว เป็น D12 ใน SPEC.md

ผู้ใช้ยืนยันเมื่อ 2026-09-14 และรับ trade-off ไว้แล้ว กฎถูกบันทึกเป็น **D12** ใน `SPEC.md` §3 ซึ่งเป็นแหล่งความจริง แผนนี้อ้างถึงเท่านั้น ไม่นิยามซ้ำ

| สถานะตาราง `departments` ตอนเริ่ม import | พฤติกรรม |
|---|---|
| ว่าง | สร้างแผนกจาก DISTINCT ของไฟล์ แล้วจึงนำเข้าพนักงาน |
| มีข้อมูลแล้ว | ไม่สร้างใหม่ แถวที่เทียบไม่ตรงถูกปฏิเสธรายแถว |

สิ่งที่ต้องระวังตอนเขียน S1 — **ตัดสินว่าตารางว่างหรือไม่ก่อนเริ่มทรานแซกชัน ไม่ใช่ระหว่างวนแถว** มิฉะนั้นแถวแรกจะสร้างแผนกแล้วทำให้แถวถัดไปเข้าเงื่อนไขคนละทาง ทำให้ผลลัพธ์ขึ้นกับลำดับแถวในไฟล์

**ไม่กระทบเวลา** — งานสองรอบ (เก็บ DISTINCT ก่อน แล้วค่อย map พนักงาน) อยู่ในงบของ mapper กับ upsert ใน S1 อยู่แล้ว เพราะ AC-I01 บังคับให้ต้องมีแผนกครบ 4 แถวหลัง import ครั้งแรกตั้งแต่ต้น

### 7.3 ความเสี่ยงเรียงตามผลกระทบ

| ความเสี่ยง | ผลถ้าเกิด | กันไว้อย่างไร |
|---|---|---|
| **buffer มีแค่ 0.2 ชม.** | อะไรพลาดสักอย่างก็ต้องสละ P0 | checkpoint 2.3 บังคับตัดสินใจที่นาฬิกา ไม่ใช่ตอนท้าย |
| Docker บน Windows ช้าหรือ volume ไม่ตรง | S0 บานเป็นสองเท่า กระทบทั้งวันแรก | ให้ S0 เป็น checkpoint แรกที่ 2.7 ชม. เลยเมื่อไรรู้ทันที |
| `exceljs` อ่านเซลล์ว่างแล้วคอลัมน์เลื่อน | ข้อมูลผิดแบบไม่มี error | บังคับอ่านด้วย cell reference และ `noUncheckedIndexedAccess` เตือนตอน compile |
| driver คืน `decimal` เป็น number | `salary` เพี้ยนทั้งระบบ ผิด D7 | มีเทสต์ยืนยันใน S5a ถ้าไม่ตรงต้องหยุดถามก่อนแก้ |
| หลงไปทำ P1 ที่ดูง่ายระหว่างทาง | เสียเวลาที่ไม่มีให้เสีย | ทุกอย่างที่เป็น P1 ถูกย้ายออกไปหัวข้อ 6.1 แล้ว หยิบได้ที่ checkpoint เท่านั้น |

---

## 8. สิ่งที่เปลี่ยนจากฉบับแรก

| # | ฉบับแรก | ฉบับนี้ |
|---|---|---|
| 1 | รายการตัดนับ S7 (1.5) และ N11/N15/N16 (0.4) เป็นเวลาที่ตัดแล้วได้คืน ทั้งที่ไม่เคยอยู่ในงบ ทำให้คิดว่าปิดส่วนเกิน 2 ชม. ได้ | แยกเป็น "รายการเพิ่ม" กับ "รายการสละ" คนละก้อน ทุกบรรทัดในรายการสละคืนเวลาจากงบจริง |
| 2 | กฎ seed แผนกไม่ถูกพูดถึงเลย จะไปเจอกลาง S1 | ตัดสินแล้วและบันทึกเป็น D12 ใน `SPEC.md` §3 แผนอ้างถึงที่ 7.2 |
| 3 | S0 ไม่มีโครงเทสต์และไม่มี `Clock` เวลา 1 ชม. ซ่อนอยู่ใน S1 | ใส่เข้า S0 ชัด ๆ (0.9) S0 เป็น 2.7 และ S1 ลดเป็น 2.5 |
| 4 | S5 เป็นก้อนเดียว 3.5 ชม. แบก P0 12 ข้อ ตัดครึ่งไม่ได้ | แยก S5a (P0 ล้วน 2.25) กับ S5b (P1 ล้วน 1.5 ไปอยู่รายการเพิ่ม) |
| 5 | ด่านหยุด 17 ด่าน ไม่มีในตารางเวลา | ยุบเหลือ 9 ด่าน และนับเวลา 1.1 ชม. ไว้ในงบเป็นบรรทัดแยก |
| 6 | S8 อยู่ท้ายสุด 1.0 ชม. หวังว่าจะมีเวลาเหลือ | ตรวจ AC ท้ายทุก slice เหลือ S8 0.5 ชม. และตั้งเส้นตายแข็งที่ T-0.5 |
| 7 | AC-L16 ไม่มี fixture, AC-I07 พิสูจน์ที่ S1 ไม่ได้, หัวคอลัมน์ Department คลิกแล้วได้ 400, `example_data/` เข้าไม่ถึง container | แก้ครบทั้งสี่จุดในเนื้อของ S0–S3 |
