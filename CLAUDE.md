# CLAUDE.md

คำสั่งสำหรับการทำงานในรีโปนี้ อ่านไฟล์นี้ก่อนแตะโค้ดทุกครั้ง

---

## 1. โปรเจกต์นี้คืออะไร

เว็บแอปจัดการข้อมูลพนักงาน ฟีเจอร์หลักสามอย่างคือ **แสดงรายการ, เรียงลำดับ/กรอง และ CRUD** ส่วน import จาก Excel เป็นเพียงวิธี seed ข้อมูลตั้งต้น ไม่ใช่ตัวสินค้า

เอกสารในรีโป เรียงตามลำดับความมีอำนาจ

| ไฟล์ | บทบาท |
|---|---|
| `SPEC.md` | **แหล่งความจริง** ว่าแอปทำอะไรได้ ขอบเขต สัญญา API กฎ validation และเกณฑ์ตรวจรับ 121 ข้อ |
| `ANALYSIS.md` | สิ่งที่พบจากไฟล์ Excel ต้นทาง และเหตุผลเบื้องหลังการตัดสินใจเรื่อง schema กับกฎ import |
| `CLAUDE.md` | ไฟล์นี้ — วิธีทำงาน ไม่ใช่ว่าจะทำอะไร |
| `PROMPTS.md` | บันทึก prompt ทุกอันตามลำดับ ต่อท้ายทุกครั้งที่ตอบ |

ถ้าโค้ดกับ `SPEC.md` ขัดกัน ให้ถือว่า spec ถูก และ **หยุดถาม** ก่อนแก้ spec ห้ามแก้ spec เพื่อให้เข้ากับโค้ดที่เขียนไปแล้ว

ก่อนเริ่มงานใด ให้ดูก่อนว่างานนั้นตรงกับเกณฑ์ AC ข้อไหนใน `SPEC.md` §7 และเป็น P0 หรือ P1 ตามตารางหัวข้อ 7.0.1 — **P0 39 ข้อคือสิ่งที่บล็อกการส่งมอบ ทำให้ครบก่อน**

---

## 2. Stack ที่ตรึงไว้

ผู้ใช้เลือกไว้แล้ว **ห้ามแทนที่ ห้ามเสนอทางเลือกอื่น ห้ามเพิ่ม library ที่ทำงานทับซ้อนกับรายการนี้**

| ส่วน | สิ่งที่ใช้ |
|---|---|
| Backend | NestJS + TypeScript **strict mode** |
| ORM | TypeORM |
| ฐานข้อมูล | PostgreSQL 16 **ตรึงเวอร์ชัน** |
| Validation | class-validator |
| Test | Jest + Supertest |
| Frontend | React + TypeScript + Vite |
| รันระบบ | Docker Compose |

ข้อบังคับเพิ่มเติม

- ทุก image ใน `docker-compose.yml` ต้องตรึงเป็น tag ที่ระบุ patch ชัดเจน เช่น `postgres:16.4-alpine` **ห้ามใช้ `:latest` และห้ามใช้ tag ลอยอย่าง `postgres:16`**
- จะเพิ่ม dependency ใหม่ที่ไม่ใช่ส่วนเสริมเล็ก ๆ ให้ถามก่อน
- ห้ามใช้ ORM อื่น query builder อื่น หรือ validation library อื่นคู่ขนานไปกับที่ระบุไว้

---

## 3. วงจรการทำงาน — Explore → Plan → Test → Implement

**กฎสำคัญที่สุดในไฟล์นี้** สำหรับโค้ดที่มีตรรกะจริง สี่ขั้นนี้เรียงลำดับตายตัว ห้ามข้าม

### ขั้นที่ 1 — Explore

ก่อนเขียนอะไรก็ตาม ให้ดูของจริงก่อน **ห้ามเดาว่าในรีโปมีอะไรอยู่แล้ว**

- เปิด `SPEC.md` หาให้ได้ว่างานนี้ตรงกับเกณฑ์ AC ข้อไหน และเป็น P0 หรือ P1
- ถ้างานแตะข้อมูล การแปลงค่า หรือ import ให้เปิด `ANALYSIS.md` ประกอบด้วย
- อ่านโค้ดที่มีอยู่ในส่วนที่จะแก้ รวมถึงเทสต์เดิมของส่วนนั้น
- สรุปสั้น ๆ ว่าเจออะไร ก่อนเสนอขั้นถัดไป

### ขั้นที่ 2 — Plan

**ถ้างานเข้าเกณฑ์ด้านล่างแม้ข้อเดียว ให้เขียนแผนแล้วหยุดถามก่อน ห้ามลงมือ**

- แตะมากกว่าหนึ่งชั้นหรือมากกว่าหนึ่ง module
- เปลี่ยน schema หรือเพิ่ม migration
- เปลี่ยนสัญญา API — เพิ่ม/ลบ/เปลี่ยนความหมายของ endpoint, field, หรือ query parameter
- กระทบ decision ข้อใดข้อหนึ่งในหัวข้อ 3 ของ `SPEC.md`
- แตะไฟล์เกินสามไฟล์
- มีวิธีทำที่สมเหตุสมผลมากกว่าหนึ่งวิธี และการเลือกผิดจะต้องรื้อ

แผนต้องบอกว่าจะแตะไฟล์ไหน ลำดับอย่างไร ครอบคลุม AC ข้อใด และมีอะไรที่จงใจไม่ทำ

งานที่เป็นการแก้จุดเดียวในชั้นเดียวและมีทางทำทางเดียว ไม่ต้องมีแผน ข้ามไปขั้นที่ 3 ได้เลย
**ถ้าไม่แน่ใจว่าต้องมีแผนหรือไม่ ให้ถือว่าต้องมี**

### ขั้นที่ 3 — Test

1. สร้าง **โครงของสิ่งที่จะทดสอบ** ก่อน คือ signature, ชนิดข้อมูล และ stub ที่ `throw new Error('not implemented')`
   จำเป็นต้องมี เพราะ TypeScript strict จะ compile เทสต์ที่อ้างถึงสิ่งที่ยังไม่มีไม่ผ่าน — stub คือการประกาศสัญญา ยังไม่ใช่ implementation
   **stub ต้องโยน error เท่านั้น ห้ามคืนค่าที่ดูสมเหตุสมผล** เช่นห้ามคืน `[]` หรือ `null` เพราะอาจทำให้เทสต์ผ่านโดยบังเอิญ
2. เขียน **เฉพาะเทสต์** ที่อธิบายพฤติกรรมที่ต้องการ ใส่เลข AC จาก `SPEC.md` §7 ไว้ในชื่อเทสต์
3. รัน **เฉพาะไฟล์เทสต์นั้นในเครื่อง** เช่น `npm test -- employees.service.spec.ts` ไม่ต้องรันทั้ง suite และไม่ต้องแตะ CI ในขั้นนี้
   ถ้าเทสต์ต้องใช้ฐานข้อมูล ให้ใช้ container ที่เปิดอยู่แล้วในเครื่อง ไม่ใช่ยกระบบขึ้นใหม่ทั้งชุด
4. แสดง output ที่ fail จริง **การ fail ที่ยอมรับได้คือ fail ที่ assertion หรือที่ `not implemented` ของ stub เท่านั้น**
   ถ้า fail เพราะ compile ไม่ผ่าน import ไม่เจอ หรือ config พัง แปลว่าขั้นที่ 1 ยังไม่เสร็จ ให้แก้ให้เรียบร้อยก่อน อย่าเพิ่งส่งมอบ
5. **หยุดจบเทิร์นตรงนี้** ให้ผู้ใช้อ่านเทสต์ก่อน

### ขั้นที่ 4 — Implement

เมื่อผู้ใช้ตอบกลับแล้วจึงเขียน implementation ที่น้อยที่สุดที่ทำให้เทสต์ผ่าน แล้วรันอีกครั้ง แสดงว่าเขียว จากนั้นจึง refactor โดยเทสต์ต้องเขียวตลอด และรันทั้ง suite หนึ่งรอบก่อนบอกว่าเสร็จ

**ห้ามเขียนเทสต์กับ implementation ในเทิร์นเดียวกัน ไม่ว่างานจะเล็กแค่ไหน**

### อะไรนับเป็น "ตรรกะจริง" — ต้องทำ TDD

- การแปลง serial → วันที่, การ normalize ค่า Status และ Department
- กฎ upsert ตาม D1 และการตัดสินว่า `updated_at` ควรขยับหรือไม่ (D1, D2)
- การรีเซ็ต identity sequence หลัง import
- validation rules ทุกข้อในหัวข้อ 5 ของ spec
- การประกอบ query ของ search / filter / sort / pagination รวมถึงการต่อ `id ASC` ท้ายทุกการเรียงลำดับ (D11)
- การแปลง error เป็นรูปแบบตามหัวข้อ 4.1
- ตรรกะฝั่ง React ที่มีเงื่อนไข เช่น การประกอบ query string จากฟอร์ม filter

### อะไรไม่ต้อง — เขียนตรงได้เลย

- scaffolding, การต่อ module, การประกาศฟิลด์ DTO ที่ไม่มีตรรกะ
- ไฟล์ config, `docker-compose.yml`, migration ที่ generate ออกมา
- layout และ styling ของ React ที่ไม่มีเงื่อนไข

ถ้าไม่แน่ใจว่าเข้าข่ายไหน **ให้ถือว่าเข้าข่ายต้องทำ TDD**

### ข้อห้าม

- ห้ามแก้เทสต์ให้อ่อนลงเพื่อให้ผ่าน ถ้าคิดว่าเทสต์เขียนผิด ให้พูดออกมาแล้วหยุด อย่าแก้เอง
- ห้าม `skip` หรือลบเทสต์ที่ fail เพื่อให้ suite เขียว
- ห้ามรายงานว่าเสร็จโดยยังไม่ได้รันเทสต์ และห้ามสรุปผลการรันจากการเดา ต้องแปะ output จริง

---

## 4. โครงสร้างโปรเจกต์

```
/
├── docker-compose.yml
├── .env.example              # commit ไฟล์นี้ ส่วน .env ห้าม commit
├── api/                      # NestJS
│   ├── src/
│   │   ├── employees/
│   │   │   ├── employees.controller.ts
│   │   │   ├── employees.service.ts
│   │   │   ├── employees.repository.ts
│   │   │   ├── employee.entity.ts
│   │   │   ├── dto/
│   │   │   └── *.spec.ts             # วางข้างไฟล์ที่มันทดสอบ
│   │   ├── departments/              # ชุดไฟล์เหมือนกันทุกประการ
│   │   ├── import/                   # ตัวอ่าน .xlsx, mapper, สคริปต์ seed
│   │   ├── common/                   # exception filter, pipe, domain error, ตัวช่วยร่วม
│   │   └── migrations/
│   └── test/                 # e2e ด้วย Supertest
├── web/                      # React + Vite
│   └── src/
│       ├── features/employees/
│       ├── features/departments/
│       └── lib/              # api client, ตัวจัดรูปแบบการแสดงผล
└── example_data/
```

เหตุผล — แยกตาม **ฟีเจอร์ ไม่ใช่ตามชั้น** เพราะ NestJS module เป็นหน่วยระดับฟีเจอร์อยู่แล้ว การแยกเป็น `controllers/` `services/` จะทำให้แก้ฟีเจอร์เดียวต้องเปิดสี่โฟลเดอร์ ส่วน `import/` แยกออกมาเพราะมันไม่ใช่ฟีเจอร์ของผู้ใช้และต้องไม่มี route ใดชี้เข้าไป (AC-I20)

เทสต์ระดับหน่วยวางไว้ข้างไฟล์ที่มันทดสอบ ส่วน e2e อยู่ใน `api/test/` เพราะต้องใช้ฐานข้อมูลจริงและรันช้ากว่า

ทุก feature folder มีสี่ไฟล์เป็นแกนเสมอ คือ controller, service, repository, entity ตามชั้นในหัวข้อ 5.1 ถ้าฟีเจอร์ไหนขาด `*.repository.ts` แปลว่ามีที่ใดที่หนึ่งกำลังข้ามชั้นอยู่

`import/` ไม่มี controller เพราะไม่มี route ใดชี้เข้าไป (AC-I20) ทางเข้าเดียวคือสคริปต์ seed ที่เรียก import service และ import service ต้องเขียนข้อมูลผ่าน repository ของ `employees` และ `departments` ไม่ใช่เปิด TypeORM ใช้เอง

---

## 5. ข้อตกลงการเขียนโค้ด

### 5.1 ชั้นของโค้ดและทิศทางการพึ่งพา

```
controller  →  service  →  repository  →  TypeORM
```

**ทางเดียวเท่านั้น** ห้ามข้ามชั้น เช่น controller เรียก repository ตรง ๆ และห้ามย้อนทิศ เช่น repository รู้จัก service

| ชั้น | หน้าที่ | ห้ามทำ |
|---|---|---|
| **Controller** | รับ HTTP, ประกาศ DTO ให้ `ValidationPipe` ตรวจ, เรียก service, แปลงผลลัพธ์เป็น response DTO, กำหนด status code และ header `Location` | ห้ามมีกฎธุรกิจแม้แต่ `if` เดียว, ห้าม import อะไรจาก `typeorm`, ห้ามประกอบ query, ห้ามส่ง entity ออกไปตรง ๆ, ห้ามดัก error เพื่อแปลงเอง — ปล่อยให้ exception filter จัดการ |
| **Service** | ตรรกะทั้งหมดของระบบ — กฎ D1/D2 และการตัดสินว่า `updated_at` ควรขยับหรือไม่, normalize ค่า, แปลง serial, ตรวจว่า department มีจริง, กำหนดขอบเขต transaction, โยน domain error | ห้ามรู้จัก HTTP เลย — ไม่มี `Request`/`Response`, ไม่โยน `HttpException` หรือ `NotFoundException`, ห้ามเขียน SQL หรือใช้ `QueryBuilder` เอง, ห้าม inject repository ของ TypeORM ตรง ๆ, ห้ามรู้ว่า response หน้าตาเป็นอย่างไร |
| **Repository** | คุยกับฐานข้อมูลอย่างเดียว — query ตาม filter/sort/pagination ที่ service สั่งมา, upsert ตาม D1, `setval`, รับ `EntityManager` เพื่อร่วม transaction ที่ service เปิดไว้, คืน entity หรือ `null` | ห้ามมีกฎธุรกิจหรือการตัดสินใจใด ๆ เช่น **ห้ามตัดสินเองว่า `updated_at` ควรเป็นค่าไหน** ให้ service ส่งค่าที่ตัดสินแล้วลงมา, ห้ามโยน `HttpException`, ห้ามรู้จัก DTO ของ HTTP |
| **TypeORM** | ชั้นล่างสุด | แตะได้เฉพาะใน `*.repository.ts`, `*.entity.ts`, `migrations/` และไฟล์ data source เท่านั้น |

วิธีตรวจว่าละเมิดหรือยัง — grep หา `from 'typeorm'` ถ้าเจอนอกสี่ที่ข้างบน แปลว่ามีการข้ามชั้น

### 5.2 ทิศทางของ error

`repository` คืน `null` หรือโยน error กลาง ๆ → `service` แปลงเป็น domain error ใน `common/errors` เช่น `DepartmentInUseError` → **exception filter ตัวเดียว** แปลง domain error เป็นรูปแบบหัวข้อ 4.1 ของ spec

เหตุผล — โค้ด NestJS ส่วนใหญ่นิยมโยน `NotFoundException` จาก service เลย ซึ่งเร็วกว่าแต่ลาก HTTP เข้าไปอยู่ในชั้นตรรกะ ทำให้สัญญา error กระจายอยู่หลายไฟล์และทดสอบ service โดยไม่พูดถึง HTTP ไม่ได้ การรวมการแปลงไว้ที่เดียวทำให้เกณฑ์ 4.1 กับ AC-N10 มีจุดตรวจเดียว แลกมาด้วยไฟล์ที่เพิ่มขึ้นหนึ่งชุด

### TypeScript

- `strict: true` และเปิด **`noUncheckedIndexedAccess`** เพิ่มด้วย
  เหตุผล — `ANALYSIS.md` ข้อค้นพบที่ 5 บอกว่าเซลล์ว่างใน `.xlsx` หายไปทั้ง element การเข้าถึงเซลล์ด้วย cell reference จึงอาจได้ `undefined` เสมอ ตัวเลือกนี้บังคับให้ compiler เตือน แทนที่จะปล่อยให้กลายเป็นบั๊กคอลัมน์เลื่อนตอน runtime
- ห้าม `any` ห้าม `!` แบบ non-null assertion และห้าม `as` ทับข้อมูลที่มาจากภายนอก (request body, แถวจาก Excel, ผลลัพธ์จาก driver) — ให้ตรวจค่าจริงแทนการหลอก compiler
- ห้ามปิด lint หรือ type error ด้วย comment ถ้าติดจริงให้หยุดถาม

### เงินและวันที่ — จุดที่พังเงียบที่สุดในโปรเจกต์นี้

- `salary` ห้ามเป็น `number` ที่จุดใดเลย ตั้งแต่ entity, DTO, ไปจนถึง JSON ที่ส่งออก ใช้ `string` ตาม D7
- `join_date` ใช้ TypeORM `type: 'date'` และถือเป็น `string` รูปแบบ `YYYY-MM-DD` ตลอดสาย **ห้ามแปลงเป็น `Date` ของ JS** ตาม D9
- การแปลง serial ให้คำนวณด้วยเลขจำนวนเต็มจาก `1899-12-30` แล้วประกอบเป็นสตริงเอง ห้ามผ่าน `Date` หรือ helper ของ library ที่ผูก timezone
- `updated_at` เป็น `timestamptz` และ **ห้ามใช้ `@UpdateDateColumn`** เพราะการ auto-stamp จะทับกฎ D1 และ D2 ทั้งคู่ ให้ service เป็นผู้เซ็ตค่าเองหลังเทียบว่าข้อมูลเปลี่ยนจริงหรือไม่
- พฤติกรรมของ driver ที่คืน `decimal` เป็น string ให้ **ยืนยันด้วยเทสต์** (AC-N06, AC-N07) ห้ามเชื่อจากเอกสารแล้วข้ามไป

### TypeORM

- `synchronize: false` เสมอ ทุกสภาพแวดล้อม รวมถึงตอน dev
  เหตุผล — schema ถูกตรึงไว้ใน `SPEC.md` §2 แล้ว การให้ ORM ปรับ schema เองจะทำให้ฐานข้อมูลจริงเบี่ยงจาก spec เงียบ ๆ และอาจลบคอลัมน์ทิ้ง
- เปลี่ยน schema ผ่าน migration เท่านั้น และ migration ต้องรันได้จากฐานข้อมูลเปล่าจนถึงสถานะปัจจุบันโดยไม่ต้องแก้มือ
- ระบุ `name:` ของทุกคอลัมน์อย่างชัดเจน เช่น `@Column({ name: 'department_id' })` ห้ามพึ่ง naming strategy แปลงให้ — `SPEC.md` §2 ระบุชื่อคอลัมน์ไว้แล้ว ต้องตรงตามนั้นแบบอ่านออกจากโค้ดได้ทันที
- `id` ต้องเป็น `GENERATED BY DEFAULT AS IDENTITY` ไม่ใช่ `ALWAYS` มิฉะนั้น import จะระบุ `id` เองไม่ได้

### Validation และ error

- ใช้ `ValidationPipe` แบบ global ตั้ง `whitelist: true`, `forbidNonWhitelisted: true`, `transform: true`
  เหตุผล — สองตัวแรกทำให้ AC-E08 และ AC-L20 (field หรือ query param ที่ไม่รู้จักต้องได้ `400`) ผ่านโดยไม่ต้องเขียนโค้ดเอง
- validation ทุกข้อต้องอยู่ฝั่ง server ฝั่ง React จะตรวจซ้ำได้เพื่อประสบการณ์ใช้งาน แต่ห้ามเป็นด่านเดียว (AC-V19)
- error ทุกก้อนออกทาง exception filter ตัวเดียว แปลงเป็นรูปแบบหัวข้อ 4.1 ของ spec รวมถึงแปลง error ของ class-validator เป็น `details[]`
- ห้ามให้ข้อความจาก driver, ชื่อ constraint หรือ stack trace หลุดออกไปใน response (AC-N10)

### React

- แสดง `salary` ด้วยรูปแบบ `#,##0.00` ที่ชั้น presentation เท่านั้น ค่าที่ถืออยู่ใน state ยังเป็น string ตามที่ API ส่งมา
- ตัวเลือกใน dropdown ของ Department ต้องมาจาก `GET /api/departments` ห้าม hardcode (AC-UI02)
- การลบต้องมีขั้นตอนยืนยัน เพราะเป็น hard delete (AC-UI07)

---

## 6. นโยบายการทดสอบ

### ระดับของเทสต์

| ระดับ | ใช้กับ | เครื่องมือ |
|---|---|---|
| Unit | ตรรกะบริสุทธิ์ — แปลง serial, normalize, ตัดสินว่า `updated_at` ขยับไหม | Jest |
| Service | กฎธุรกิจที่ต้องมีตัวช่วยจากชั้นล่าง โดยใส่ repository ปลอมแบบ in-memory เข้าไปแทน | Jest |
| Integration / e2e | ทุกอย่างที่แตะฐานข้อมูลหรือ HTTP — CRUD, filter, pagination, upsert, error shape | Jest + Supertest ยิงใส่ NestJS app จริง |

repository ปลอมในระดับ service ทำได้เพราะชั้นถูกแยกไว้ตามหัวข้อ 5.1 แล้ว **แต่ใช้ได้เฉพาะการทดสอบกฎธุรกิจเท่านั้น** พฤติกรรมของ repository เองห้ามทดสอบด้วยของปลอม ดูข้อถัดไป

### ห้าม mock ฐานข้อมูล

เทสต์ที่เกี่ยวกับ repository ต้องรันกับ **PostgreSQL 16 จริงใน container** ห้ามใช้ sqlite ห้าม mock repository

เหตุผล — ข้อกำหนดเกือบทั้งหมดในโปรเจกต์นี้ขึ้นกับพฤติกรรมของ Postgres โดยเฉพาะ `DECIMAL(12,2)`, `TIMESTAMPTZ`, `DATE`, `ON CONFLICT ... WHERE`, `IS DISTINCT FROM` และ identity sequence ฐานข้อมูลจำลองจะทำให้เทสต์เขียวโดยไม่ได้พิสูจน์อะไรเลย ซึ่งแย่กว่าการไม่มีเทสต์เพราะให้ความมั่นใจปลอม

### ความครอบคลุม

- **เกณฑ์ P0 ทั้ง 39 ข้อต้องมีเทสต์อัตโนมัติ** ชื่อเทสต์ขึ้นต้นด้วยเลข AC เช่น
  `it('AC-I05: "In Active" ต้องบันทึกเป็น is_active = false', ...)`
  เหตุผล — ทำให้เทสต์ที่ fail ชี้กลับไปยังหัวข้อใน spec ได้ทันที และตรวจได้ว่าข้อไหนยังไม่มีคนเขียนเทสต์
- P1 เขียนเทสต์เมื่อทำได้ง่าย ถ้าเลือกไม่เขียนให้บอกออกมาว่าข้อไหน
- **ไม่ตั้งเป้าเป็นเปอร์เซ็นต์ coverage** เป้าหมายคือ P0 ครบทุกข้อ ตัวเลข coverage สูงจากเทสต์ที่ไม่ assert อะไรไม่มีความหมาย

### ความเสถียรของเทสต์

- ตั้ง `TZ=UTC` ในสภาพแวดล้อมของเทสต์เป็นค่าเริ่มต้น
- แต่ **ต้องมีเทสต์อย่างน้อยหนึ่งตัวที่รัน import ด้วย `TZ` ที่ไม่ใช่ UTC** เช่น `Asia/Bangkok` และ `America/Los_Angeles` แล้วยืนยันว่าได้วันเดียวกัน (AC-I04) ถ้าทุกเทสต์รันใน UTC หมด บั๊กวันเลื่อนจะไม่มีวันถูกจับได้
- เวลาปัจจุบันในเทสต์ต้องหยุดไว้หรือควบคุมได้ ห้าม assert กับ `new Date()` ตรง ๆ
- แต่ละเทสต์ต้องรันซ้ำได้และไม่ขึ้นกับลำดับ ให้ล้างหรือรีเซ็ตข้อมูลระหว่างเทสต์

---

## 7. การรันระบบ

- ทุกอย่างรันผ่าน Docker Compose — service `db`, `api`, `web`
- `db` ต้องมี healthcheck และ `api` ต้องรอจนกว่า `db` จะพร้อม ไม่ใช่รอด้วย `sleep`
- ข้อมูลฐานข้อมูลเก็บใน named volume ไม่ใช่ bind mount
- ค่า config อ่านจาก environment ทั้งหมด `.env.example` ต้อง commit และมีคีย์ครบ ส่วน `.env` จริงห้าม commit
- **import รันเป็นคำสั่ง one-off เท่านั้น** เช่น `docker compose run --rm api npm run seed` ห้ามมี HTTP endpoint หรือปุ่มใน UI ที่รับไฟล์ (AC-I20) และต้องรัน `setval` ทุกครั้งหลัง import
- ห้าม commit ไฟล์ข้อมูลจริง credential หรือ dump ฐานข้อมูล

---

## 8. Git

- **ห้าม commit และห้าม push เว้นแต่ผู้ใช้สั่งตรง ๆ** และห้ามลงท้ายการตอบด้วยการเสนอตัวจะ commit ให้ ปล่อยงานที่แก้แล้วไว้ใน working tree ตามปกติ
- เมื่อได้รับคำสั่งให้ commit
  - ใช้รูปแบบ conventional commit เช่น `feat:`, `fix:`, `test:`, `chore:`
  - แยก commit ของเทสต์กับ implementation ออกจากกัน ให้ประวัติสะท้อนลำดับ TDD ที่เกิดขึ้นจริง
  - ห้ามใช้ `--no-verify` และห้ามข้าม hook ถ้า hook fail ให้แก้ต้นเหตุ
- ห้ามสร้าง branch, rebase, หรือ force push โดยไม่ได้สั่ง

---

## 9. การสื่อสาร

- **ตอบเป็นภาษาไทยเสมอ** แม้ผู้ใช้จะพิมพ์มาเป็นภาษาอังกฤษ ชื่อตัวแปร ชื่อคอลัมน์ ชนิดข้อมูล คำสั่ง และ path ให้คงเป็นภาษาอังกฤษตามปกติของงานเทคนิค
- **ต่อท้าย `PROMPTS.md` หนึ่งรายการหลังตอบทุกครั้ง โดยไม่ต้องรอให้สั่ง** คัดลอก prompt มาแบบคำต่อคำ ห้ามย่อหรือเรียบเรียงใหม่ แล้วสรุปผลลัพธ์ 1–2 บรรทัด ใช้เลขลำดับต่อเนื่องแบบเติมศูนย์
- รายงานผลตามจริง เทสต์ fail ให้บอกพร้อม output ข้ามขั้นตอนไหนให้บอกว่าข้าม
- เจอข้อขัดแย้งใน spec หรือเจอสิ่งที่ต้องเดา ให้หยุดถาม ไม่ใช่เดาแล้วเขียนต่อ
