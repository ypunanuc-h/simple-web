# SPEC — Employee Management System (ems)

เว็บแอปสำหรับจัดการข้อมูลพนักงาน ประกอบด้วยหน้ารายการที่ค้นหาและกรองได้ กับการจัดการข้อมูลครบวงจร (CRUD)

เอกสารนี้กำหนด **สิ่งที่แอปทำได้** เท่านั้น ไม่ระบุ framework, library หรือโครงสร้างโปรเจกต์ — เรื่องเหล่านั้นอยู่ใน `CLAUDE.md`
ส่วน schema ของฐานข้อมูลและกฎการ import อ้างอิงจาก [ANALYSIS.md](ANALYSIS.md) และเอกสารนี้จะระบุชัดทุกจุดที่แก้ไขข้อสรุปเดิม

---

## 1. ขอบเขต

### 1.1 อยู่ในขอบเขต

| ความสามารถ | รายละเอียด |
|---|---|
| List view | ตารางพนักงาน แบ่งหน้าฝั่ง server เรียงลำดับตามคอลัมน์ได้ |
| Search | ค้นหาข้อความอิสระจาก `name` แบบ partial ไม่สนตัวพิมพ์เล็กใหญ่ |
| Filter | department, status, ช่วง `join_date` (from/to), ช่วง `salary` (min/max) ใช้ร่วมกันได้ทั้งหมด |
| Employee CRUD | สร้าง / อ่าน / แก้ไข / ลบพนักงาน — **ลบคือลบแถวออกจริง (hard delete)** |
| Department CRUD | สร้าง / อ่าน / แก้ไข / ลบแผนก — ลบไม่ได้ถ้ายังมีพนักงานอ้างอยู่ (ตอบ `409`) |
| Seed จาก Excel | คำสั่งฝั่ง server สำหรับ import `example_data/exam_data.xlsx` ครั้งแรกและครั้งถัดไป ทำงานตาม [ANALYSIS.md §3](ANALYSIS.md) |

**ฟีเจอร์หลักคือสามอย่างแรก** — แสดงรายการ, เรียงลำดับ/กรอง และ CRUD พนักงาน ส่วน Department CRUD กับ Seed จาก Excel อยู่ในขอบเขตและต้องทำ แต่เป็นเรื่องรองที่ไม่ได้ตัดสินว่าแอปใช้งานได้หรือไม่ ลำดับความสำคัญนี้สะท้อนอยู่ในเกณฑ์ตรวจรับหัวข้อ 7.0.1

หน้าจอที่ต้องมี

- **หน้ารายการ** — ตาราง + แถบ search/filter + ตัวควบคุมการแบ่งหน้า + ปุ่มสร้างพนักงานใหม่
- **ฟอร์มพนักงาน** — ใช้ร่วมกันทั้งการสร้างและการแก้ไข ตาม UI control ในหัวข้อ 2
- **กล่องยืนยันการลบ** — ต้องยืนยันก่อนเสมอ เพราะเป็น hard delete ที่กู้คืนไม่ได้
- **หน้าจัดการแผนก** — รายการแผนกพร้อม CRUD

### 1.2 นอกขอบเขต

ทุกข้อด้านล่างคือ **การตัดสินใจ ไม่ใช่สิ่งที่หลงลืม**

| สิ่งที่ตัดออก | เหตุผล |
|---|---|
| Authentication | โจทย์เป็นเครื่องมือจัดการข้อมูลภายใน ยังไม่มีข้อกำหนดว่าใครเป็นผู้ใช้ จึงยังไม่มีอะไรให้ยืนยันตัวตน — ดูข้อจำกัดการ deploy ในหัวข้อ 6.6 |
| User roles / permissions | สิทธิ์ต้องมีตัวตนผู้ใช้ก่อน เมื่อไม่มี auth ระบบสิทธิ์จึงไม่มีอะไรให้ยึด |
| Audit trail | `updated_at` ตอบคำถามว่า "แถวนี้เปลี่ยนล่าสุดเมื่อไร" ซึ่งเพียงพอสำหรับโจทย์นี้ ส่วนตาราง history เต็มรูปแบบต้องรู้ก่อนว่า "ใคร" เป็นคนเปลี่ยน ซึ่งกลับไปติดที่ไม่มี auth |
| อัปโหลดไฟล์จาก UI | Excel เป็นเพียงวิธี seed ข้อมูลตั้งต้น ไม่ใช่ตัวสินค้า การ import จึงเป็นคำสั่งฝั่ง server ที่รันโดยผู้ดูแลระบบ ไม่ใช่ฟีเจอร์ของผู้ใช้ |
| Export | ไม่มีข้อกำหนดปลายทางว่าต้องส่งข้อมูลออกไปที่ใดในรูปแบบใด การเดารูปแบบไฟล์ล่วงหน้าจะได้ของที่ไม่มีใครใช้ |
| Soft delete / ถังขยะ | ผู้ใช้เลือก hard delete ไว้แล้ว การเก็บแถวที่ลบแล้วต้องเพิ่มคอลัมน์และเงื่อนไขกรองในทุก query โดยไม่มีความต้องการรองรับ |
| Optimistic locking / ตรวจจับการแก้ชนกัน | ไม่มี auth แปลว่ายังไม่รู้จำนวนผู้ใช้พร้อมกัน ระบบจึงเป็น last-write-wins ตามที่ระบุในหัวข้อ 6.5 |
| Bulk edit / bulk delete | ชุดข้อมูลตั้งต้นมี 5 แถว และยังไม่มีขั้นตอนการทำงานที่ต้องแก้หลายแถวพร้อมกัน |
| คอลัมน์สกุลเงิน และวันที่เริ่ม inactive | ตกลงว่าอยู่นอกขอบเขตไว้แล้วใน [ANALYSIS.md §5](ANALYSIS.md) ยกมาบันทึกไว้ที่นี่เพื่อไม่ให้ตกหล่น |

---

## 2. Data dictionary

### 2.1 `employees`

| คอลัมน์ใน Excel | field name | DB type | UI control |
|---|---|---|---|
| ID | `id` | `INT GENERATED BY DEFAULT AS IDENTITY` PK | read-only (แสดงในตารางและหัวฟอร์มแก้ไข ไม่มีในฟอร์มสร้าง) |
| Name | `name` | `VARCHAR(255) NOT NULL` | text input |
| Department | `department_id` | `INT NOT NULL` FK → `departments(id)` | dropdown (ดึงตัวเลือกจาก `GET /api/departments`) |
| Salary | `salary` | `DECIMAL(12,2) NOT NULL` | number input (แสดงผลรูปแบบ `#,##0.00` เป็นหน้าที่ FE) |
| Join Date | `join_date` | `DATE NOT NULL` | date picker |
| Status | `is_active` | `BOOLEAN NOT NULL DEFAULT true` | checkbox (ติ๊ก = Active) |
| Last Updated Date | `updated_at` | `TIMESTAMPTZ NOT NULL` | read-only (แสดงเป็นเวลา UTC) |

### 2.2 `departments`

| คอลัมน์ใน Excel | field name | DB type | UI control |
|---|---|---|---|
| — (seed จาก DISTINCT ของคอลัมน์ Department) | `id` | `INT GENERATED BY DEFAULT AS IDENTITY` PK | read-only |
| Department | `name` | `VARCHAR(100) NOT NULL UNIQUE` | text input |

DDL ตาม [ANALYSIS.md §2](ANALYSIS.md) ไม่มีการเปลี่ยนแปลง

---

## 3. Decisions

### D1 — `updated_at` ตอน re-import: stamp `now()` เฉพาะเมื่อค่าเปลี่ยนจริง

ข้อนี้ **ปิดคำถามที่ค้างไว้ใน [ANALYSIS.md §3.4](ANALYSIS.md)** และแทนที่กฎเดิมที่ให้ค่าใน DB ชนะเสมอ

| กรณี | `updated_at` ที่ได้ |
|---|---|
| แถวใหม่ | ค่าจากไฟล์ (serial → เที่ยงคืน UTC ของวันนั้น) |
| แถวเดิม ค่าข้อมูลไม่เปลี่ยนเลย | ค่าเดิมใน DB (ไม่มีการเขียนทับใด ๆ) |
| แถวเดิม ค่าข้อมูลเปลี่ยนจริงอย่างน้อยหนึ่งคอลัมน์ | `now()` |

```sql
INSERT INTO employees (id, name, department_id, salary, join_date, is_active, updated_at)
VALUES (...)
ON CONFLICT (id) DO UPDATE SET
  name          = EXCLUDED.name,
  department_id = EXCLUDED.department_id,
  salary        = EXCLUDED.salary,
  join_date     = EXCLUDED.join_date,
  is_active     = EXCLUDED.is_active,
  updated_at    = now()
WHERE (employees.name, employees.department_id, employees.salary,
       employees.join_date, employees.is_active)
      IS DISTINCT FROM
      (EXCLUDED.name, EXCLUDED.department_id, EXCLUDED.salary,
       EXCLUDED.join_date, EXCLUDED.is_active);
```

เงื่อนไข `WHERE` ต้องเทียบเฉพาะคอลัมน์ข้อมูล **ห้ามรวม `updated_at`** มิฉะนั้นแถวที่ไฟล์แก้แค่วันที่จะถูกนับว่าเปลี่ยน และเงื่อนไขจะกลายเป็นจริงเสมอ

สิ่งที่ได้และสิ่งที่ต้องรับไว้

- `updated_at` เดินหน้าอย่างเดียว ไม่ถอยหลัง และสะท้อนการเปลี่ยนแปลงที่มาจาก import จริง — แก้ปัญหา stale stamp ที่ ANALYSIS.md เตือนไว้
- import ไฟล์เดิมซ้ำกลายเป็น idempotent โดยสมบูรณ์ รอบที่สองไม่แตะแถวใดเลย
- **ผลที่ยอมรับ** — `Last Updated Date` ในไฟล์มีผลกับแถวใหม่เท่านั้น ถ้าแก้เฉพาะช่องวันที่ในไฟล์ของแถวที่มีอยู่แล้ว ค่านั้นจะไม่ถูกนำเข้า เพราะระบบถือว่า import คือ "การแก้ข้อมูลรูปแบบหนึ่ง" และ stamp ตามเวลาที่แก้จริง

### D2 — API ก็ stamp เฉพาะเมื่อค่าเปลี่ยนจริงเช่นกัน

`PUT /api/employees/{id}` ที่ส่งค่าเดิมกลับมาทุกช่องจะไม่ขยับ `updated_at` ใช้ตรรกะเดียวกับ D1 เพื่อให้ stamp มีความหมายเดียวตลอดทั้งระบบ ไม่ว่าข้อมูลจะเข้ามาทางไหน การกดบันทึกโดยไม่แก้อะไรจึงไม่ทำให้แถวนั้นดูเหมือนเพิ่งถูกแตะ

`POST` เซ็ต `updated_at = now()` เสมอ และไคลเอนต์ส่งค่า `updated_at` มาเองไม่ได้ทุกกรณี

### D3 — Delete พนักงานคือลบแถวออกจริง

`DELETE` ลบแถวถาวร ไม่มีการเก็บซาก UI ต้องมีขั้นตอนยืนยันก่อนเสมอ

**ผลที่ยอมรับ** — การ upsert ใช้ `id` จากไฟล์เป็น conflict target ดังนั้นถ้าลบพนักงานที่มาจากไฟล์ แล้วรัน import ไฟล์เดิมอีกครั้ง แถวนั้นจะกลับมา ซึ่งถูกต้องตามนิยามที่ว่าไฟล์คือแหล่งข้อมูลตั้งต้น ไม่ใช่บั๊ก

### D4 — `is_active` เป็นสถานะ ไม่ใช่การลบ

การติ๊ก checkbox ออกคือการบอกว่าพนักงานไม่ได้ทำงานแล้ว แถวยังอยู่ครบและยังปรากฏในรายการเมื่อไม่ได้กรองสถานะ ไม่ควรใช้แทนการลบ

### D5 — Department ลบไม่ได้ถ้ายังถูกอ้างอยู่

`department_id` เป็น `NOT NULL` การลบแผนกที่ยังมีพนักงานอยู่จึงไม่มีค่าปลายทางที่ถูกต้องให้ย้ายไป ระบบตอบ `409` พร้อมจำนวนพนักงานที่ยังอ้างอยู่ และให้ผู้ใช้ย้ายพนักงานเองก่อน ตรงไปตรงมาและตรงกับข้อจำกัดของ FK

### D6 — ชื่อแผนกซ้ำตัดสินด้วยกฎ normalize เดียวกับ import

เทียบด้วย **ตัดช่องว่างหัวท้าย → ลบช่องว่างภายใน → แปลงเป็นตัวพิมพ์เล็ก** ตาม [ANALYSIS.md §3.2](ANALYSIS.md) ดังนั้น `"Engineering"`, `"engineering"` และ `"Engi neering"` ถือเป็นแผนกเดียวกันและถูกปฏิเสธด้วย `409` ถ้ากฎในแอปกับกฎใน import ต่างกัน ผู้ใช้จะสร้างแผนกที่ import มองไม่เห็นได้

ค่าที่ **เก็บจริง** คือข้อความที่ผู้ใช้พิมพ์หลังตัดช่องว่างหัวท้ายเท่านั้น การ normalize ใช้เพื่อเปรียบเทียบ ไม่ใช่เพื่อแปลงค่าก่อนบันทึก

### D7 — `salary` รับส่งเป็น string ใน JSON

`"65000.00"` ไม่ใช่ `65000.00` เพราะ JSON number ฝั่ง JavaScript คือ IEEE-754 double การผ่านชนิดนั้นทำให้ `DECIMAL(12,2)` ที่ตั้งใจไว้หลุดความแม่นยำได้โดยไม่มีสัญญาณเตือน ซึ่งเป็นปัญหาแบบเดียวกับที่ ANALYSIS.md ระวังไว้ตลอดทั้งฉบับ FE ต้อง format เอง จึงต้องแปลงค่าอยู่แล้วไม่ว่าทางใด

### D8 — แก้ไขด้วย `PUT` แบบส่งค่ามาทั้งชุด ไม่มี `PATCH`

ฟอร์มแก้ไขส่งทุกช่องกลับมาอยู่แล้ว การมีเส้นทางแก้ไขทางเดียวทำให้ validation, การเทียบค่าเปลี่ยนใน D2 และการทดสอบมีชุดเดียว ไม่ต้องนิยามความหมายของ "ช่องที่ไม่ได้ส่งมา"

### D9 — วันที่ในสัญญา API เป็นสตริง ไม่ใช่ object เวลา

`join_date` เป็น `YYYY-MM-DD` และ `updated_at` เป็น ISO-8601 ลงท้าย `Z` เสมอ ทั้งสองฝั่งห้ามแปลงผ่านชนิดเวลาที่ผูก timezone ของเครื่องก่อนส่งหรือก่อนบันทึก ตามข้อค้นพบที่ 2 ใน [ANALYSIS.md §1](ANALYSIS.md) ที่ว่าความเสี่ยงวันเลื่อน 1 วันเกิดตอนแปลงชนิด ไม่ใช่ตอน parse

### D10 — `id` มาจากระบบเสมอ

ไคลเอนต์ส่ง `id` มาใน body ไม่ได้ทั้ง `POST` และ `PUT` เส้นทางเดียวที่ระบุ `id` เองได้คือ import และต้องรัน `setval` หลัง import ทุกครั้งตาม [ANALYSIS.md §3.5](ANALYSIS.md) เพื่อให้พนักงานที่สร้างใหม่คนถัดไปได้ `id` = `106`

### D11 — การเรียงลำดับต้องเสถียร

ทุกการเรียงลำดับต่อท้ายด้วย `id ASC` เสมอ ถ้าไม่ทำ แถวที่มีค่าเท่ากัน เช่น เงินเดือนเท่ากัน อาจสลับตำแหน่งระหว่างหน้า ทำให้ข้อมูลบางแถวหายหรือซ้ำเมื่อเปลี่ยนหน้า

### D12 — แผนกถูกสร้างจากไฟล์ได้เฉพาะตอนตาราง `departments` ยังว่าง

ข้อนี้ปิดความขัดแย้งระหว่างหัวข้อ 2.2 ที่ระบุว่า `departments` seed จาก DISTINCT ของคอลัมน์ Department กับ [ANALYSIS.md §3.2](ANALYSIS.md) และ AC-I11 ที่ห้ามสร้างแผนกใหม่อัตโนมัติ ถ้ายึดข้อใดข้อหนึ่งเพียงลำพัง จะมีเกณฑ์ตรวจรับตกเสมอ

| สถานะของตาราง `departments` ตอนเริ่ม import | พฤติกรรม |
|---|---|
| ว่าง | สร้างแผนกจาก DISTINCT ของคอลัมน์ Department ในไฟล์ แล้วจึงนำเข้าพนักงาน |
| มีข้อมูลอยู่แล้ว | ไม่สร้างแผนกใหม่ใด ๆ แถวที่ normalize แล้วเทียบไม่ตรงกับแผนกที่มีอยู่ **ถูกปฏิเสธรายแถว** ตาม `ANALYSIS.md` §3.2 |

การตัดสินว่าตารางว่างหรือไม่ ให้ดูที่สถานะก่อนเริ่มทรานแซกชันของ import รอบนั้น ไม่ใช่ระหว่างวนแถว มิฉะนั้นแถวแรกจะสร้างแผนกแล้วทำให้แถวถัด ๆ ไปเข้าเงื่อนไขคนละทาง

**ผลที่ยอมรับ** — คำที่พิมพ์ผิดในคอลัมน์ Department ของไฟล์ **ตอน import ครั้งแรกครั้งเดียว** จะกลายเป็นแผนกจริง เพราะรอบนั้นไฟล์คือสิ่งที่กำหนด master list ตามเจตนาของหัวข้อ 2.2 หลังจากนั้นกลไกกันคำพิมพ์ผิดของ `ANALYSIS.md` §3.2 ทำงานเต็มรูปแบบ วิธีแก้ถ้าเกิดขึ้นจริงคือลบแผนกที่ผิดทิ้งแล้ว import ใหม่ ไม่ใช่เพิ่มเงื่อนไขพิเศษในตัว import

เกณฑ์ตรวจรับไม่เปลี่ยน — **AC-I01** ผ่านเพราะการ import ครั้งแรกจากฐานข้อมูลว่างสร้างแผนกครบ 4 และ **AC-I11** ผ่านเพราะตอนนั้นตารางมีข้อมูลแล้ว แถว `"Enginering"` จึงถูกปฏิเสธและไม่มีแผนกใหม่เกิดขึ้น

---

## 4. API contract

### 4.1 ข้อตกลงร่วม

- Base path `/api` รับส่งเป็น JSON (`application/json; charset=utf-8`)
- เวลาทุกค่าเป็น UTC `updated_at` อยู่ในรูป `2026-01-10T00:00:00Z`
- พารามิเตอร์ที่ไม่รู้จักใน query string ให้ **ปฏิเสธด้วย `400`** ไม่ใช่เงียบ ๆ ข้ามไป เพราะการพิมพ์ชื่อ filter ผิดแล้วได้ผลลัพธ์ครบทุกแถวคือผลลัพธ์ที่หลอกตา
- field ที่ไม่รู้จักใน request body ให้ปฏิเสธด้วย `400` ด้วยเหตุผลเดียวกัน

รูปแบบ error เหมือนกันทุก endpoint

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "ข้อมูลที่ส่งมาไม่ถูกต้อง",
    "details": [
      { "field": "salary", "rule": "max_scale", "message": "ทศนิยมได้ไม่เกิน 2 ตำแหน่ง" }
    ]
  }
}
```

| code | HTTP | ใช้เมื่อ |
|---|---|---|
| `VALIDATION_ERROR` | 400 | body หรือ query ผิดกฎในหัวข้อ 5 |
| `NOT_FOUND` | 404 | ไม่พบ `id` ที่อ้างถึง |
| `DUPLICATE_NAME` | 409 | ชื่อแผนกซ้ำตามกฎ D6 |
| `DEPARTMENT_IN_USE` | 409 | ลบแผนกที่ยังมีพนักงานอ้างอยู่ |
| `INTERNAL_ERROR` | 500 | ข้อผิดพลาดที่ไม่ได้คาดไว้ ห้ามส่งรายละเอียดภายในออกไป |

### 4.2 Employee resource

```json
{
  "id": 101,
  "name": "John Doe",
  "department": { "id": 1, "name": "Engineering" },
  "salary": "65000.00",
  "join_date": "2023-01-15",
  "is_active": true,
  "updated_at": "2026-01-10T00:00:00Z"
}
```

การตอบกลับฝัง object `department` มาให้เลย เพื่อให้ตารางแสดงชื่อแผนกได้โดยไม่ต้องยิงตามทีละแถว ส่วนการ **ส่งเข้า** ใช้ `department_id` ตัวเลขเท่านั้น

### 4.3 `GET /api/employees`

| พารามิเตอร์ | ชนิด | ค่าเริ่มต้น | ความหมาย |
|---|---|---|---|
| `q` | string | — | ค้นหาจาก `name` แบบ partial ไม่สนตัวพิมพ์เล็กใหญ่ ตัดช่องว่างหัวท้ายก่อนใช้ ถ้าว่างหลังตัดถือว่าไม่ได้ส่งมา |
| `department_id` | int | — | กรองทีละหนึ่งแผนก ต้องมีอยู่จริง |
| `is_active` | `true` / `false` | — | ไม่ส่ง = ทุกสถานะ |
| `join_date_from` | `YYYY-MM-DD` | — | รวมวันที่ระบุ (`>=`) |
| `join_date_to` | `YYYY-MM-DD` | — | รวมวันที่ระบุ (`<=`) |
| `salary_min` | decimal string | — | `>=` |
| `salary_max` | decimal string | — | `<=` |
| `sort` | enum | `id` | `id`, `name`, `department`, `salary`, `join_date`, `updated_at` — `department` เรียงตามชื่อแผนก |
| `order` | `asc` / `desc` | `asc` | |
| `page` | int ≥ 1 | `1` | |
| `page_size` | int 1–100 | `20` | เกิน 100 ตอบ `400` ไม่ใช่ปรับลงให้เงียบ ๆ |

เงื่อนไขทุกตัวรวมกันแบบ AND ค่าที่ไม่ได้ส่งมาถือว่าไม่กรอง

```
GET /api/employees?q=jo&department_id=1&is_active=true
      &salary_min=50000&salary_max=70000
      &join_date_from=2022-01-01&join_date_to=2024-12-31
      &sort=salary&order=desc&page=1&page_size=20
```

`200 OK`

```json
{
  "data": [ { "id": 101, "name": "John Doe", "...": "..." } ],
  "meta": { "page": 1, "page_size": 20, "total": 1, "total_pages": 1 }
}
```

หน้าที่เลยจำนวนข้อมูลตอบ `200` พร้อม `data` เป็นลิสต์ว่าง ไม่ใช่ `404` เพราะการขอหน้าที่ไม่มีข้อมูลไม่ใช่การขอทรัพยากรที่ไม่มีอยู่

### 4.4 Employee endpoints

| Method | Path | Body | สำเร็จ | ล้มเหลว |
|---|---|---|---|---|
| `GET` | `/api/employees/{id}` | — | `200` employee | `404` |
| `POST` | `/api/employees` | `name`, `department_id`, `salary`, `join_date`, `is_active?` | `201` + employee + header `Location` | `400` |
| `PUT` | `/api/employees/{id}` | `name`, `department_id`, `salary`, `join_date`, `is_active` (ครบทุกช่อง) | `200` employee | `400`, `404` |
| `DELETE` | `/api/employees/{id}` | — | `204` ไม่มี body | `404` |

```json
POST /api/employees
{
  "name": "Dana Lee",
  "department_id": 1,
  "salary": "61000.00",
  "join_date": "2026-09-01",
  "is_active": true
}
```

`is_active` ไม่ส่งมาใน `POST` ได้ ค่าเริ่มต้นคือ `true` ตาม DDL แต่ใน `PUT` ต้องส่งเสมอเพราะเป็นการแทนค่าทั้งชุดตาม D8

`DELETE` ที่ `id` เดิมซ้ำครั้งที่สองตอบ `404` ตามความเป็นจริงว่าไม่มีแถวนั้นแล้ว

### 4.5 Department endpoints

| Method | Path | Body | สำเร็จ | ล้มเหลว |
|---|---|---|---|---|
| `GET` | `/api/departments` | — | `200` ลิสต์เรียงตาม `name` ไม่แบ่งหน้า | — |
| `POST` | `/api/departments` | `name` | `201` + department | `400`, `409 DUPLICATE_NAME` |
| `PUT` | `/api/departments/{id}` | `name` | `200` + department | `400`, `404`, `409 DUPLICATE_NAME` |
| `DELETE` | `/api/departments/{id}` | — | `204` | `404`, `409 DEPARTMENT_IN_USE` |

`GET` ไม่แบ่งหน้าเพราะปลายทางคือ dropdown ที่ต้องได้ตัวเลือกครบทุกครั้ง จำนวนแผนกอยู่ในหลักสิบ

```json
GET /api/departments → 200
[
  { "id": 1, "name": "Engineering", "employee_count": 2 },
  { "id": 4, "name": "HR",          "employee_count": 1 }
]
```

`employee_count` มีไว้ให้หน้าจัดการแผนกบอกล่วงหน้าได้ว่าแผนกไหนลบไม่ได้ แทนที่จะให้ผู้ใช้ไปเจอ `409` เอง

```json
DELETE /api/departments/1 → 409
{
  "error": {
    "code": "DEPARTMENT_IN_USE",
    "message": "แผนกนี้มีพนักงาน 2 คนอ้างอยู่ ย้ายพนักงานออกก่อนจึงจะลบได้",
    "details": [ { "field": "id", "rule": "in_use", "message": "employee_count = 2" } ]
  }
}
```

### 4.6 Operational endpoint

| Method | Path | Body | สำเร็จ | ล้มเหลว |
|---|---|---|---|---|
| `GET` | `/api/health` | — | `200` `{"status":"ok"}` | — |

endpoint เดียวในเอกสารนี้ที่ไม่ใช่ข้อมูลของผู้ใช้ มีไว้ให้คนหรือเครื่องมือถามว่า API ยังตอบอยู่หรือไม่ โดยไม่ต้องแตะข้อมูลจริง **ห้ามคืนอะไรมากกว่า `status`** เช่นเวอร์ชัน ชื่อโฮสต์ หรือสถานะการต่อฐานข้อมูล เพราะจะกลายเป็นการเปิดเผยรายละเอียดภายในซึ่งขัดกับ 6.4 และไม่ต้องยืนยันตัวตนอยู่แล้วตาม 1.2

ข้อนี้ไม่มีเกณฑ์ตรวจรับในหัวข้อ 7 เพราะเป็นเครื่องมือปฏิบัติการ ไม่ใช่ความสามารถของแอป

---

## 5. Validation rules

ทุกกฎบังคับใช้ที่ฝั่ง server เสมอ ส่วนฝั่ง FE บังคับซ้ำได้เพื่อประสบการณ์ใช้งาน แต่ห้ามเป็นด่านเดียว

### 5.1 Employee

| field | กฎ | rule code |
|---|---|---|
| `name` | ต้องมี ตัดช่องว่างหัวท้ายก่อนตรวจ ยาว 1–255 ตัวอักษร ห้ามว่างหลังตัด | `required`, `length` |
| `department_id` | ต้องมี เป็นจำนวนเต็ม และต้องมีอยู่จริงใน `departments` | `required`, `not_found` |
| `salary` | ต้องมี เป็นตัวเลขทศนิยมในรูป string ทศนิยมไม่เกิน 2 ตำแหน่ง ค่า `>= 0` และ `<= 9999999999.99` ห้ามมีคอมมาคั่นหลักหรือสัญลักษณ์สกุลเงิน | `required`, `format`, `max_scale`, `range` |
| `join_date` | ต้องมี รูปแบบ `YYYY-MM-DD` เป็นวันที่จริงตามปฏิทิน และอยู่ในช่วง `1900-01-01` ถึง `2100-12-31` | `required`, `format`, `range` |
| `is_active` | ต้องเป็น boolean แท้ ไม่รับ `"true"`, `1`, `"Active"` — บังคับใน `PUT` ไม่บังคับใน `POST` (ค่าเริ่มต้น `true`) | `required`, `type` |
| `id` | ห้ามมีใน body | `not_allowed` |
| `updated_at` | ห้ามมีใน body | `not_allowed` |

ขอบล่าง `1900-01-01` ของ `join_date` ล้อกับกฎ import ที่ปฏิเสธ serial น้อยกว่า `61` เพื่อกันบั๊กปี 1900 ให้ทั้งสองทางเข้าข้อมูลมีขอบเขตเดียวกัน

ระบบ **ไม่ห้าม** `join_date` ที่เป็นวันในอนาคต เพราะการบันทึกพนักงานที่จะเริ่มงานเดือนหน้าเป็นเรื่องปกติ

ระบบ **ไม่มี** เงินเดือนขั้นต่ำเชิงธุรกิจ มีแต่ขอบเขตทางเทคนิคของ `DECIMAL(12,2)` — หากต้องการขั้นต่ำจริง ต้องระบุเพิ่มภายหลัง

### 5.2 Department

| field | กฎ | rule code |
|---|---|---|
| `name` | ต้องมี ตัดช่องว่างหัวท้ายก่อนตรวจ ยาว 1–100 ตัวอักษร | `required`, `length` |
| `name` | ห้ามซ้ำกับแผนกอื่นเมื่อเทียบด้วยกฎ normalize ใน D6 | `duplicate` |

### 5.3 Query parameters

| กฎ |
|---|
| `page`, `page_size`, `department_id` ต้องเป็นจำนวนเต็มบวก |
| `page_size` เกิน 100 ตอบ `400` |
| `sort` ต้องอยู่ใน whitelist ในหัวข้อ 4.3 เท่านั้น ห้ามนำค่าจากผู้ใช้ไปต่อเป็น SQL ตรง ๆ |
| `join_date_from` ต้องไม่มากกว่า `join_date_to` และ `salary_min` ต้องไม่มากกว่า `salary_max` |
| `is_active` รับเฉพาะ `true` หรือ `false` |

### 5.4 Import

รายละเอียดเต็มอยู่ที่ [ANALYSIS.md §3](ANALYSIS.md) สรุปสิ่งที่ต้องคงไว้

- อ่านจาก `.xlsx` โดยตรง **ห้ามมีขั้นตอนแปลงเป็น CSV คั่นกลาง** และอ้างเซลล์ด้วย cell reference เสมอ
- serial → วันที่ คำนวณจาก `1899-12-30` ส่งต่อเป็นสตริง ไม่ผ่านชนิดเวลาที่ผูก timezone และปฏิเสธ serial ที่น้อยกว่า `61`
- Status และ Department normalize ด้วยกฎเดียวกัน ค่าที่เทียบไม่ตรง **ปฏิเสธรายแถว** ห้ามใส่ค่า default และห้ามสร้างแผนกใหม่อัตโนมัติ — ยกเว้นกรณีเดียวคือตาราง `departments` ยังว่างตอนเริ่ม import ตาม D12
- แถวที่ถูกปฏิเสธให้ข้าม ไม่ล้มทั้งไฟล์ และ log อย่างน้อย เลขแถวในชีต, `ID`, เหตุผลสั้น ๆ
- upsert ตาม D1 แล้วรัน `setval` ทุกครั้งหลัง import

---

## 6. Non-functional

### 6.1 ประสิทธิภาพ

- ห้ามมี endpoint ใดคืนพนักงานทั้งตารางโดยไม่แบ่งหน้า การนับ `total` ต้องนับที่ฐานข้อมูล ไม่ใช่ดึงมานับในแอป
- สมมติฐานขนาดข้อมูลที่ออกแบบรองรับคือ **≤ 100,000 แถว** ซึ่งเกินความจำเป็นของไฟล์ตั้งต้นมาก แต่กำหนดไว้เพื่อให้เลือกใช้ index ได้อย่างมีเหตุผล
- เป้าหมาย `GET /api/employees` ที่ขนาดข้อมูลข้างต้นคือตอบภายใน 300 ms ที่ p95 โดยวัดจากฝั่ง server — เป็น **เป้าหมายการออกแบบ ไม่ใช่เกณฑ์ที่ตรวจในรอบนี้** เพราะยังไม่มีข้อมูลและเครื่องมือวัดที่จะรันได้ ดูหัวข้อ 7.9 ประกอบ
- index ที่ต้องมี

```sql
CREATE INDEX ON employees (department_id);
CREATE INDEX ON employees (is_active);
CREATE INDEX ON employees (join_date);
CREATE INDEX ON employees (salary);
CREATE INDEX ON employees (lower(name));
```

- การค้นหาแบบ contains (`%คำค้น%`) ใช้ index ปกติไม่ได้ ถ้าข้อมูลโตจนช้าจริงให้เปลี่ยนไปใช้ trigram index ไม่ใช่เปลี่ยนพฤติกรรมการค้นหา

### 6.2 ความถูกต้องของข้อมูล

- เงินเดือนต้องเป็นชนิดทศนิยมแม่นยำตลอดเส้นทาง DB → API → FE ห้ามผ่าน float ที่จุดใดจุดหนึ่ง
- `TIMESTAMPTZ` และ `DATE` ต้องคงตามที่กำหนด การเปลี่ยนชนิดเหล่านี้ถือเป็นการเปลี่ยน spec
- ทุกคอลัมน์เป็น `NOT NULL` ตามที่ตกลงไว้ ไม่มี field เป็น optional

### 6.3 เวลาและโซนเวลา

- ค่าที่เก็บและค่าที่ส่งออกทาง API เป็น UTC ทั้งหมด
- FE แปลงเป็นเวลาท้องถิ่นเพื่อแสดงผลได้ แต่ห้ามส่งค่าที่แปลงแล้วกลับเข้าระบบ
- `join_date` ไม่ใช่ timestamp ห้ามแปลงผ่านชนิดเวลาที่ผูก timezone ที่ชั้นใดก็ตาม

### 6.4 การจัดการข้อผิดพลาด

- ทุก error ใช้รูปแบบเดียวกันในหัวข้อ 4.1 และมี `code` ที่อ่านด้วยเครื่องได้
- ข้อความ error ที่ส่งถึงผู้ใช้ต้องบอกว่าต้องแก้อะไร ไม่ใช่ stack trace หรือข้อความจาก driver ฐานข้อมูล
- ข้อผิดพลาดฝั่ง server ต้อง log พร้อมข้อมูลที่ตามรอยได้ แต่ห้ามหลุดออกไปใน response

### 6.5 การใช้งานพร้อมกัน

Last-write-wins โดยตั้งใจ ตามที่ระบุไว้ในขอบเขต 1.2 ถ้าสองคนแก้พนักงานคนเดียวกัน คนที่บันทึกทีหลังชนะ และ `updated_at` จะสะท้อนการบันทึกครั้งนั้น ระบบไม่แจ้งเตือนการชนกัน

### 6.6 ข้อจำกัดในการ deploy

เมื่อไม่มี auth ตามข้อ 1.2 ทุกคนที่เข้าถึงแอปได้มีสิทธิ์แก้และลบข้อมูลพนักงานทั้งหมด ระบบนี้จึงต้อง deploy ในเครือข่ายภายในที่ควบคุมการเข้าถึงแล้วเท่านั้น **ห้ามเปิดสู่อินเทอร์เน็ตสาธารณะ** จนกว่าจะมีการเพิ่ม auth

ข้อนี้เป็น **เงื่อนไขก่อนขึ้นระบบ ไม่ใช่เกณฑ์ตรวจรับแอป** เพราะเป็นคุณสมบัติของการตั้งค่าเครือข่าย ไม่ใช่พฤติกรรมของโค้ด ตัวแอปจะเขียนดีแค่ไหนก็ทำให้ข้อนี้ผ่านเองไม่ได้ ผู้ที่ deploy เป็นผู้ยืนยันก่อนเปิดใช้งานจริง

### 6.7 การทำงานของ import

- รันเป็นคำสั่งฝั่ง server โดยผู้ดูแลระบบ ไม่มีทางเข้าจาก UI
- รันซ้ำได้อย่างปลอดภัย ไฟล์เดิมที่ import ซ้ำจะไม่เปลี่ยนข้อมูลใด ๆ ตาม D1
- ผลการรันต้องสรุปให้เห็นจำนวนแถวที่เพิ่ม แก้ไข ไม่เปลี่ยน และถูกข้าม พร้อมเหตุผลของแถวที่ข้าม

### 6.8 เบราว์เซอร์

รองรับเบราว์เซอร์รุ่นปัจจุบันบนเดสก์ท็อป (Chrome, Edge, Firefox, Safari) หน้ารายการต้องใช้งานได้ที่ความกว้างหน้าจอเดสก์ท็อปทั่วไปโดยไม่ต้องเลื่อนแนวนอน

ข้อควรรู้ — โปรเจกต์นี้พัฒนาบน Windows ซึ่งติดตั้ง Safari ไม่ได้ การรองรับ WebKit จึงเป็น **คำแถลงการรองรับที่ยังไม่ได้ทดสอบ** ไม่ใช่สิ่งที่ยืนยันแล้ว ดูหัวข้อ 7.9 ห้ามใช้ฟีเจอร์ CSS หรือ JS ที่รู้อยู่แล้วว่า WebKit ไม่รองรับ

---

## 7. Acceptance criteria

### 7.0 วิธีใช้หัวข้อนี้

ทุกข้อต้องตัดสิน **ผ่าน / ไม่ผ่าน** ได้ด้วยการทำตามที่เขียนไว้แล้วดูผล ไม่มีข้อใดที่ต้องใช้ดุลยพินิจ เกณฑ์ชุดนี้ใช้ได้ทั้งเป็นรายการตรวจด้วยมือและเป็นรายการ test case อัตโนมัติ

เกณฑ์ส่วนใหญ่ตรวจจากสิ่งที่สังเกตได้จากภายนอก คือ HTTP response หรือหน้าจอ ข้อที่เท่านี้ไม่พอจะมีป้ายกำกับไว้ที่ต้นข้อ

- `[DB]` ต้องเปิดดูฐานข้อมูลโดยตรง เช่น ชนิดคอลัมน์ รายการ index หรือ query plan
- `[CODE]` ต้องอ่านโค้ดหรือ config เพราะเป็นข้อห้ามเชิงโครงสร้างที่ไม่ทิ้งร่องรอยให้เห็นจากภายนอกเมื่อทุกอย่างทำงานถูกต้อง

ทุกข้อมีเลขอ้างอิงกำกับกลับไปยังหัวข้อหรือ decision ที่เป็นที่มา ถ้าเกณฑ์ข้อใดขัดกับ spec ให้ถือว่า spec เป็นตัวตั้งและเกณฑ์ข้อนั้นผิด

เกณฑ์ที่ต้องใช้เครื่องมือหรือสภาพแวดล้อมซึ่ง build นี้ยังไม่มี ถูกแยกไปไว้ในหัวข้อ **7.9** และ **ไม่นับรวม** ในการตรวจรับ

### 7.0.1 ลำดับความสำคัญ

แอปนี้เป็นเว็บแอปขนาดเล็กที่มีฟีเจอร์หลักสามอย่างคือ **แสดงรายการข้อมูล, เรียงลำดับ/กรอง และ CRUD** เกณฑ์จึงแบ่งเป็นสองระดับ

- **P0 — ต้องผ่าน** ตรงกับฟีเจอร์หลักสามอย่างข้างต้น รวมกับข้อที่ถ้าพลาดแล้วข้อมูลผิดตั้งแต่ต้นหรือมีช่องโหว่ ถ้าข้อใดข้อหนึ่งไม่ผ่าน ถือว่าแอปยังใช้งานไม่ได้
- **P1 — ควรผ่าน แต่ไม่บล็อก** เป็นความเรียบร้อย ความทนทาน และกรณีขอบ บันทึกไว้ว่าไม่ผ่านได้ แล้วค่อยตามแก้

สามหัวข้อที่เป็น **เรื่องรองทั้งหัวข้อ** คือ 7.3 (Department CRUD), 7.6 (Import) และ 7.8 (Non-functional) ยังอยู่ในขอบเขตและยังต้องทำ แต่ไม่ใช่สิ่งที่ตัดสินว่าแอปใช้งานได้หรือไม่ ยกเว้นข้อที่ยกขึ้นเป็น P0 ไว้ในตารางด้านล่าง

| หัวข้อ | P0 — ต้องผ่าน | ที่เหลือเป็น P1 |
|---|---|---|
| 7.1 List / search / filter / sort | AC-L01, L02, L03, L05, L07–L14, L16, L21, L25 | L04, L06, L15, L17, L18, L19, L20, L22, L23, L24 |
| 7.2 Employee CRUD | AC-E01, E02, E03, E04, E09, E11, E13 | E05, E06, E07, E08, E10, E12 |
| 7.3 Department CRUD | AC-D01 | D02–D14 |
| 7.4 Validation | AC-V01, V04, V05, V19 | V02, V03, V06–V18 |
| 7.5 `updated_at` | AC-U01, U03 | U02, U04, U05, U06 |
| 7.6 Import | AC-I01, I02, I03, I05, I07 | I04, I06, I08–I20 |
| 7.7 UI | AC-UI01, UI02, UI04, UI07 | UI03, UI05, UI06, UI08, UI09, UI10 |
| 7.8 Non-functional | AC-N04 | N01, N03, N05–N12, N14, N15, N16 |

รวม P0 39 ข้อ จากทั้งหมด 121 ข้อ

เหตุผลของข้อที่อาจดูไม่เข้าพวก

- **AC-L21** (`sort` ต้องอยู่ใน whitelist) เป็น P0 เพราะเป็นช่องทาง SQL injection ไม่ใช่เรื่องความเรียบร้อย
- **AC-V19** (ปิด validation ฝั่ง FE แล้วยิงตรงเข้า API) เป็น P0 ด้วยเหตุผลเดียวกัน
- **AC-V04** (`department_id` ที่ไม่มีอยู่ต้องได้ `400` ไม่ใช่ `500`) เป็น P0 เพราะ `500` จาก FK violation คือข้อผิดพลาดที่ผู้ใช้แก้เองไม่ได้
- **AC-I01, I02, I03, I05, I07** เป็น P0 ทั้งที่ import เป็นเรื่องรอง เพราะห้าข้อนี้ตัดสินว่า **ข้อมูลตั้งต้นถูกต้องหรือไม่** ถ้าเงินเดือนกลายเป็น `65`, วันที่เลื่อนไปหนึ่งวัน หรือ `"In Active"` กลายเป็น `true` หน้ารายการที่ทำมาอย่างดีก็แสดงข้อมูลผิดตั้งแต่แถวแรก ส่วนกรณีขอบของ import ที่เหลือเป็น P1 ทั้งหมด
- **AC-N04** (เลขหน้าถูกต้องที่ 25 แถว) เป็น P0 เพราะเป็นความถูกต้องของการแสดงรายการ ไม่ใช่เรื่องประสิทธิภาพ

**สถานะตั้งต้นของการตรวจ (baseline)** — ฐานข้อมูลว่าง แล้วรัน import `example_data/exam_data.xlsx` หนึ่งครั้ง ผลที่ได้คือพนักงาน `101`–`105` และแผนก `Engineering`, `Marketing`, `Sales`, `HR` ตามภาคผนวกของ [ANALYSIS.md](ANALYSIS.md) ทุกข้อด้านล่างเริ่มจากสถานะนี้เสมอ เว้นแต่ระบุเป็นอย่างอื่น

### 7.1 List view, search, filter, sort, pagination

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-L01 | `GET /api/employees` ไม่ส่งพารามิเตอร์ใดเลย | `200` `data` 5 แถว `meta` = page 1, page_size 20, total 5, total_pages 1 | 4.3 |
| AC-L02 | `?q=jo` | เจอ `John Doe` | 4.3 |
| AC-L03 | `?q=JOHN` และ `?q=john` | ได้ผลลัพธ์ชุดเดียวกับ AC-L02 | 4.3 |
| AC-L04 | `?q=%20%20jo%20%20` (มีช่องว่างหัวท้าย) | ได้ผลลัพธ์ชุดเดียวกับ AC-L02 | 4.3 |
| AC-L05 | `?q=` (ว่าง) | เท่ากับไม่ได้ส่ง `q` คือได้ครบ 5 แถว ไม่ใช่ 0 แถว | 4.3 |
| AC-L06 | `?q=engineering` | ได้ 0 แถว เพราะค้นจาก `name` เท่านั้น ไม่ใช่ชื่อแผนก | 4.3 |
| AC-L07 | `?department_id=<id ของ Engineering>` | ได้ `101` และ `104` | 4.3 |
| AC-L08 | `?is_active=false` | ได้เฉพาะ `104` | 4.3 |
| AC-L09 | ไม่ส่ง `is_active` | ได้ทั้ง active และ inactive | 4.3 |
| AC-L10 | `?salary_min=50000&salary_max=65000` | ได้ `101`, `102`, `105` โดยรวมค่าขอบทั้งสองด้าน | 4.3 |
| AC-L11 | `?join_date_from=2024-01-01` | ได้ `103` และ `105` | 4.3 |
| AC-L12 | `?join_date_to=2023-01-15` | ได้ `101` และ `104` โดยรวมวันที่ `2023-01-15` ด้วย | 4.3 |
| AC-L13 | `?department_id=<Engineering>&is_active=true&salary_min=60000` | ได้เฉพาะ `101` แสดงว่าเงื่อนไขรวมกันแบบ AND ไม่ใช่ OR | 4.3 |
| AC-L14 | `?sort=salary&order=desc` | เรียง `104, 101, 102, 105, 103` | 4.3 |
| AC-L15 | `?sort=department` | เรียงตาม **ชื่อแผนก** ไม่ใช่ `department_id` | 4.3 |
| AC-L16 | มีพนักงานเงินเดือนเท่ากันหลายคน แล้วไล่ดูทีละหน้าด้วย `?sort=salary&page_size=2` | ไม่มีแถวใดหายหรือปรากฏซ้ำข้ามหน้า และลำดับคงเดิมเมื่อเรียกซ้ำ | D11 |
| AC-L17 | `?page=99` | `200` พร้อม `data` เป็นลิสต์ว่าง **ไม่ใช่** `404` | 4.3 |
| AC-L18 | `?page_size=101` | `400 VALIDATION_ERROR` **ไม่ใช่** ปรับลงเป็น 100 ให้เงียบ ๆ | 4.3, 5.3 |
| AC-L19 | `?page_size=0` หรือ `?page=0` | `400 VALIDATION_ERROR` | 5.3 |
| AC-L20 | `?deparment_id=1` (สะกดผิด) | `400 VALIDATION_ERROR` **ไม่ใช่** `200` พร้อมข้อมูลครบทุกแถว | 4.1 |
| AC-L21 | `?sort=password` หรือ `?sort=id;DROP TABLE employees` | `400 VALIDATION_ERROR` ไม่ใช่ `500` และตารางยังอยู่ครบ | 5.3 |
| AC-L22 | `?salary_min=70000&salary_max=50000` | `400 VALIDATION_ERROR` | 5.3 |
| AC-L23 | `?join_date_from=2024-12-31&join_date_to=2024-01-01` | `400 VALIDATION_ERROR` | 5.3 |
| AC-L24 | `?is_active=1` หรือ `?is_active=yes` | `400 VALIDATION_ERROR` | 5.3 |
| AC-L25 | ทุก response ของรายการ | มี `department` เป็น object `{id, name}` ในทุกแถว โดยไม่ต้องเรียก endpoint อื่นเพิ่ม | 4.2 |

### 7.2 Employee CRUD

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-E01 | `GET /api/employees/101` | `200` พร้อมฟิลด์ครบตามหัวข้อ 4.2 | 4.4 |
| AC-E02 | `GET /api/employees/999` | `404 NOT_FOUND` | 4.4 |
| AC-E03 | `POST` ข้อมูลถูกต้องครบ | `201` พร้อม header `Location` และ body ที่มี `id` ที่ระบบกำหนดให้ | 4.4 |
| AC-E04 | `POST` โดยไม่ส่ง `is_active` | `201` และแถวที่ได้มี `is_active = true` | 4.4, 5.1 |
| AC-E05 | `PUT` โดยไม่ส่ง `is_active` | `400 VALIDATION_ERROR` เพราะ `PUT` แทนค่าทั้งชุด | D8, 5.1 |
| AC-E06 | `POST` หรือ `PUT` ที่มี `"id"` อยู่ใน body | `400 VALIDATION_ERROR` rule `not_allowed` | D10, 5.1 |
| AC-E07 | `POST` หรือ `PUT` ที่มี `"updated_at"` อยู่ใน body | `400 VALIDATION_ERROR` rule `not_allowed` | D2, 5.1 |
| AC-E08 | `POST` ที่มี field แปลกปลอม เช่น `"email"` | `400 VALIDATION_ERROR` | 4.1 |
| AC-E09 | `DELETE /api/employees/103` | `204` ไม่มี body และ `GET /api/employees/103` ตอบ `404` ต่อจากนั้น | 4.4, D3 |
| AC-E10 | `DELETE /api/employees/103` ซ้ำครั้งที่สอง | `404 NOT_FOUND` | 4.4 |
| AC-E11 | หลังลบ `103` แล้ว `GET /api/employees` | `meta.total` เหลือ 4 แสดงว่าแถวหายไปจริง ไม่ใช่ถูกซ่อน | D3 |
| AC-E12 | `[DB]` ตรวจฐานข้อมูลหลัง `DELETE` | ไม่มีแถว `id = 103` หลงเหลือในตาราง และไม่มีคอลัมน์สถานะการลบใด ๆ | D3, 1.2 |
| AC-E13 | ตั้ง `is_active = false` ผ่าน `PUT` | แถวยังอยู่ และยังปรากฏใน `GET /api/employees` ที่ไม่ได้กรองสถานะ | D4 |

### 7.3 Department CRUD (เรื่องรอง)

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-D01 | `GET /api/departments` | `200` ได้ 4 แผนก เรียงตามชื่อ ไม่มี `meta` การแบ่งหน้า | 4.5 |
| AC-D02 | `GET /api/departments` | ทุกแถวมี `employee_count` และค่าตรงกับจำนวนพนักงานจริง (Engineering = 2, ที่เหลืออย่างละ 1) | 4.5 |
| AC-D03 | `POST` ชื่อ `"Finance"` | `201` และปรากฏใน dropdown ของฟอร์มพนักงานทันทีโดยไม่ต้อง deploy ใหม่ | 4.5, D5 |
| AC-D04 | `POST` ชื่อ `"engineering"` ทั้งที่มี `"Engineering"` อยู่แล้ว | `409 DUPLICATE_NAME` | D6 |
| AC-D05 | `POST` ชื่อ `"Engi neering"` | `409 DUPLICATE_NAME` เพราะ normalize แล้วตรงกัน | D6 |
| AC-D06 | `POST` ชื่อ `"  Finance  "` แล้วอ่านกลับ | ค่าที่เก็บคือ `"Finance"` ตัดช่องว่างหัวท้ายแล้ว แต่ **ไม่ถูกแปลงเป็นตัวพิมพ์เล็ก** | D6 |
| AC-D07 | `POST` ชื่อ `"Data Science"` | `201` และค่าที่เก็บคือ `"Data Science"` ที่ยังมีช่องว่างตรงกลางครบ | D6 |
| AC-D08 | `DELETE` แผนก `Engineering` ที่มีพนักงาน 2 คน | `409 DEPARTMENT_IN_USE` และ `details` บอกจำนวนพนักงานที่อ้างอยู่ | D5, 4.5 |
| AC-D09 | หลังจาก AC-D08 | พนักงาน `101` และ `104` ยังอยู่ครบ และแผนก `Engineering` ยังอยู่ | D5 |
| AC-D10 | ย้ายพนักงานออกจากแผนกจนหมดแล้ว `DELETE` | `204` | D5 |
| AC-D11 | `DELETE` แผนกที่ไม่มีอยู่ | `404 NOT_FOUND` | 4.5 |
| AC-D12 | `PUT` เปลี่ยนชื่อแผนกเป็นชื่อที่ชนกับแผนกอื่นตามกฎ normalize | `409 DUPLICATE_NAME` | D6 |
| AC-D13 | `PUT` เปลี่ยนชื่อแผนกเป็นชื่อเดิมของตัวเอง | `200` ไม่ใช่ `409` | D6 |
| AC-D14 | เปลี่ยนชื่อแผนกแล้วเรียก `GET /api/employees` | แถวที่อ้างแผนกนั้นแสดงชื่อใหม่ โดยไม่ต้องแก้ข้อมูลพนักงาน | 2.1 |

### 7.4 Validation

| ID | ข้อมูลที่ส่ง | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-V01 | `name` เป็น `""` หรือ `"   "` | `400` rule `required` หรือ `length` | 5.1 |
| AC-V02 | `name` ยาว 256 ตัวอักษร | `400` rule `length` | 5.1 |
| AC-V03 | `name` เป็น `"  John  "` | `201`/`200` และค่าที่เก็บคือ `"John"` | 5.1 |
| AC-V04 | `department_id` เป็น `9999` ที่ไม่มีอยู่ | `400` rule `not_found` **ไม่ใช่** `500` จาก FK violation | 5.1 |
| AC-V05 | `salary` เป็น `"65,000.00"` | `400` rule `format` **ไม่ถูกตีความเป็น 65** | 5.1, ANALYSIS ปัญหาข้อ 10 |
| AC-V06 | `salary` เป็น `"65000.999"` | `400` rule `max_scale` | 5.1 |
| AC-V07 | `salary` เป็น `"-1"` | `400` rule `range` | 5.1 |
| AC-V08 | `salary` เป็น `"10000000000.00"` (เกิน `DECIMAL(12,2)`) | `400` rule `range` ไม่ใช่ error จากฐานข้อมูล | 5.1 |
| AC-V09 | `salary` เป็น `"0"` | สำเร็จ เพราะ spec ไม่ได้กำหนดขั้นต่ำเชิงธุรกิจ | 5.1 |
| AC-V10 | `salary` เป็น `"$65000"` | `400` rule `format` | 5.1 |
| AC-V11 | `join_date` เป็น `"2023-02-30"` | `400` rule `format` เพราะไม่ใช่วันที่จริงตามปฏิทิน | 5.1 |
| AC-V12 | `join_date` เป็น `"15-Jan-23"` หรือ `"01/15/2023"` | `400` rule `format` | 5.1, D9 |
| AC-V13 | `join_date` เป็น `"1899-12-31"` | `400` rule `range` ล้อกับกฎปฏิเสธ serial < 61 ของ import | 5.1 |
| AC-V14 | `join_date` เป็นวันในอนาคต เช่น เดือนหน้า | สำเร็จ ไม่ใช่ `400` | 5.1 |
| AC-V15 | `is_active` เป็น `"true"` (string) หรือ `1` | `400` rule `type` | 5.1 |
| AC-V16 | `is_active` เป็น `"Active"` | `400` rule `type` — คำจากไฟล์ Excel ต้องไม่เข้าทาง API | 5.1, ANALYSIS ปัญหาข้อ 1 |
| AC-V17 | ทุก error ที่เกิดจาก validation | body ตรงรูปแบบหัวข้อ 4.1 คือมี `error.code`, `error.message`, `error.details[]` ที่ระบุ `field` และ `rule` | 4.1 |
| AC-V18 | ส่งข้อมูลผิดพร้อมกันหลายช่อง | `details` รายงาน **ทุกช่องที่ผิด** ไม่ใช่หยุดที่ช่องแรก | 4.1 |
| AC-V19 | ปิดการตรวจสอบฝั่ง FE แล้วยิงข้อมูลผิดตรงเข้า API | ยังถูกปฏิเสธเหมือนเดิม | 5 |

### 7.5 `updated_at`

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-U01 | `POST` สร้างพนักงานใหม่ | `updated_at` คือเวลาปัจจุบัน ไม่ใช่ `null` และไม่ใช่ค่าที่ไคลเอนต์กำหนด | D2 |
| AC-U02 | `PUT` โดยส่งค่าเดิมกลับมาทุกช่อง | `updated_at` **ไม่เปลี่ยน** | D2 |
| AC-U03 | `PUT` แก้ `salary` | `updated_at` เปลี่ยนเป็นเวลาปัจจุบัน | D2 |
| AC-U04 | `PUT` แก้เฉพาะ `is_active` | `updated_at` เปลี่ยน | D2 |
| AC-U05 | ค่าที่ส่งออกทาง API | อยู่ในรูป ISO-8601 ลงท้ายด้วย `Z` เช่น `2026-01-10T00:00:00Z` | D9, 6.3 |
| AC-U06 | เปรียบเทียบค่าก่อนและหลังทุกการกระทำในหัวข้อนี้ | `updated_at` ไม่เคยมีค่าน้อยลงกว่าเดิม | D1 |

### 7.6 Import / seed (เรื่องรอง)

เกณฑ์กลุ่มนี้ตรวจสิ่งที่ [ANALYSIS.md §3](ANALYSIS.md) กำหนดไว้ ข้อที่ต้องใช้ไฟล์ดัดแปลงให้ทำสำเนาไฟล์ต้นฉบับแล้วแก้เฉพาะจุดที่ระบุ

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-I01 | import ครั้งแรกจากฐานข้อมูลว่าง | ได้พนักงาน 5 แถว (`101`–`105`) และแผนก 4 แถว | ANALYSIS ภาคผนวก |
| AC-I02 | ตรวจ `salary` ของ `101` | `65000.00` **ไม่ใช่** `65` | ANALYSIS ปัญหาข้อ 10 |
| AC-I03 | ตรวจ `join_date` ของ `101` | `2023-01-15` พอดี ไม่ใช่ `2023-01-14` หรือ `2023-01-16` | ANALYSIS ข้อค้นพบ 2 |
| AC-I04 | รัน import ซ้ำโดยตั้ง `TZ` ของเครื่องเป็น `Asia/Bangkok` รอบหนึ่ง และ `America/Los_Angeles` อีกรอบหนึ่ง | `join_date` ทุกแถวเท่ากันทั้งสองรอบ | ANALYSIS ข้อค้นพบ 2, 6.3 |
| AC-I05 | ตรวจ `is_active` ของ `104` ที่ไฟล์เขียนว่า `"In Active"` | `false` — ข้อนี้คือกับดักหลักของชุดข้อมูล ถ้าได้ `true` ถือว่าไม่ผ่านทันที | ANALYSIS ปัญหาข้อ 1 |
| AC-I06 | ตรวจ `updated_at` ของ `101` หลัง import ครั้งแรก | `2026-01-10T00:00:00Z` คือเที่ยงคืน UTC ของวันในไฟล์ | D1, ANALYSIS §3.1 |
| AC-I07 | สร้างพนักงานใหม่ผ่าน API หลัง import | ได้ `id` = `106` ไม่ใช่ `1` และไม่เกิด error เรื่อง PK ซ้ำ | D10, ANALYSIS §3.5 |
| AC-I08 | รัน import ไฟล์เดิมซ้ำครั้งที่สอง | ไม่มีแถวเพิ่ม ไม่มีแถวใดที่ `updated_at` เปลี่ยน และรายงานสรุปบอกว่าแก้ไข 0 แถว | D1 |
| AC-I09 | แก้ `Salary` ของแถว `101` ในไฟล์เป็น `70000` แล้ว import | `101` ได้ `salary` = `70000.00` และ `updated_at` เป็นเวลาที่รัน import ส่วนแถว `102`–`105` `updated_at` ไม่ขยับ | D1 |
| AC-I10 | แก้ **เฉพาะ** `Last Updated Date` ของแถว `101` ในไฟล์แล้ว import | `updated_at` ของ `101` ไม่เปลี่ยน เพราะไฟล์ไม่มีผลกับคอลัมน์นี้ในแถวที่มีอยู่แล้ว | D1 |
| AC-I11 | เพิ่มแถวที่ `Department` เป็น `"Enginering"` (พิมพ์ผิด) แล้ว import | แถวนั้นถูกข้าม แถวอื่น commit ตามปกติ และ **ไม่มี** แผนกใหม่ชื่อ `Enginering` เกิดขึ้น | ANALYSIS §3.2 |
| AC-I12 | เพิ่มแถวที่ `Status` เป็น `"Retired"` แล้ว import | แถวนั้นถูกข้าม ไม่ถูกใส่ค่า default เป็น `true` | ANALYSIS §3.2 |
| AC-I13 | เพิ่มแถวที่ `Status` เป็น `"  in active  "` แล้ว import | นำเข้าสำเร็จโดยได้ `is_active = false` เพราะ normalize แล้วตรงกับ `inactive` | ANALYSIS §3.2 |
| AC-I14 | ลบค่าในเซลล์ `Salary` ของแถวหนึ่งให้ว่าง แล้ว import | แถวนั้นถูกข้าม และแถวถัดไป **ไม่เลื่อนคอลัมน์** คือชื่อยังตรงกับชื่อ เงินเดือนยังตรงกับเงินเดือน | ANALYSIS ข้อค้นพบ 5 |
| AC-I15 | ทุกแถวที่ถูกข้าม | log มีอย่างน้อย เลขแถวในชีต, `ID`, และเหตุผลสั้น ๆ | ANALYSIS §3.3 |
| AC-I16 | รายงานสรุปท้ายการ import | บอกจำนวนแถวที่เพิ่ม แก้ไข ไม่เปลี่ยน และถูกข้าม | 6.7 |
| AC-I17 | ไฟล์ที่มีแถวเสีย 1 แถวจาก 6 แถว | อีก 5 แถว commit สำเร็จ ไม่ล้มทั้งไฟล์ | ANALYSIS §3.3 |
| AC-I18 | ลบพนักงาน `103` ผ่าน API แล้ว import ไฟล์เดิมอีกครั้ง | `103` กลับมา — เป็นพฤติกรรมที่ตั้งใจตาม D3 ไม่ใช่ข้อบกพร่อง | D3 |
| AC-I19 | `[CODE]` ตรวจเส้นทางการอ่านไฟล์ | ไม่มีขั้นตอนแปลงเป็น CSV คั่นกลางในกระบวนการ import | ANALYSIS ปัญหาข้อ 10 |
| AC-I20 | `[CODE]` ตรวจรายการ route ทั้งหมดและหน้าจอทุกหน้า | ไม่มี endpoint หรือปุ่มใดที่รับไฟล์อัปโหลดได้ | 1.2, 6.7 |

### 7.7 UI

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-UI01 | ดูคอลัมน์ Salary ในตาราง | แสดงเป็น `65,000.00` ตามรูปแบบ `#,##0.00` | 2.1 |
| AC-UI02 | ดูช่อง Department ในฟอร์ม | เป็น dropdown ที่ตัวเลือกมาจาก `GET /api/departments` ไม่ใช่รายการที่ hardcode ไว้ | 2.1 |
| AC-UI03 | เพิ่มแผนกใหม่ผ่านหน้าจัดการแผนก แล้วกลับมาที่ฟอร์มพนักงาน | แผนกใหม่ปรากฏใน dropdown | 4.5 |
| AC-UI04 | ดูช่อง Status ในฟอร์ม | เป็น checkbox โดยติ๊ก = Active | 2.1 |
| AC-UI05 | เปิดฟอร์มสร้างพนักงานใหม่ | ไม่มีช่องให้กรอก ID และไม่มีช่อง Last Updated Date ที่แก้ไขได้ | 2.1, D10 |
| AC-UI06 | เปิดฟอร์มแก้ไข | ID และ Last Updated Date แสดงเป็น read-only | 2.1 |
| AC-UI07 | กดปุ่มลบ | ต้องมีขั้นตอนยืนยันก่อน และการยกเลิกต้องไม่ลบข้อมูล | 1.1, D3 |
| AC-UI08 | ส่งฟอร์มที่มีข้อมูลผิด | ข้อความผิดพลาดชี้ไปที่ช่องที่ผิด และบอกว่าต้องแก้อะไร ไม่ใช่ข้อความดิบจากระบบ | 6.4 |
| AC-UI09 | ใช้ search ร่วมกับ filter แล้วเปลี่ยนหน้า | เงื่อนไขที่เลือกไว้ยังคงอยู่ ไม่ถูกล้างเมื่อเปลี่ยนหน้า | 1.1 |
| AC-UI10 | ค้นหาแล้วไม่พบข้อมูล | แสดงสถานะว่าง ไม่ใช่หน้าจอเปล่าหรือข้อความ error | 4.3 |

### 7.8 Non-functional (เรื่องรอง)

| ID | เงื่อนไข / การกระทำ | ผลที่ต้องได้ | อ้างอิง |
|---|---|---|---|
| AC-N01 | `[CODE]` ตรวจทุก endpoint ที่คืนรายการพนักงาน | ไม่มี endpoint ใดคืนทั้งตารางโดยไม่แบ่งหน้า | 6.1 |
| AC-N03 | `[DB]` ตรวจรายการ index ของตาราง `employees` | มีครบตามรายการในหัวข้อ 6.1 | 6.1 |
| AC-N04 | สร้างพนักงานจนมี 25 แถว แล้วเรียก `?page_size=10&page=3` | `meta.total` = 25, `meta.total_pages` = 3, `data` มี 5 แถว และไม่มีแถวใดซ้ำกับสองหน้าก่อนหน้า | 6.1, 4.3 |
| AC-N05 | `[DB]` ตรวจชนิดคอลัมน์ในตารางจริง | ตรงกับหัวข้อ 2 ทุกคอลัมน์ โดยเฉพาะ `DECIMAL(12,2)`, `DATE`, `TIMESTAMPTZ` | 6.2 |
| AC-N06 | ตรวจชนิดของ `salary` ใน JSON response | เป็น string `"65000.00"` ไม่ใช่ number | D7 |
| AC-N07 | บันทึกค่า `salary` ที่มีเศษสตางค์ เช่น `"65000.10"` แล้วอ่านกลับ | ได้ `"65000.10"` พอดี ไม่คลาดเคลื่อน | 6.2, D7 |
| AC-N08 | เปลี่ยน timezone ของเครื่องที่เปิดหน้าเว็บ | `join_date` ที่แสดงไม่เปลี่ยนวัน | 6.3, D9 |
| AC-N09 | `[DB]` ตรวจทุกคอลัมน์ในตาราง | ทุกคอลัมน์เป็น `NOT NULL` ไม่มีคอลัมน์ที่ไม่ได้ระบุใน spec เช่นคอลัมน์สำหรับ soft delete | 6.2, 1.2 |
| AC-N10 | ทำให้เกิดข้อผิดพลาดฝั่ง server เช่นหยุดฐานข้อมูลแล้วเรียก API | ตอบ `500` ที่มีรูปแบบ error ตามหัวข้อ 4.1 และ **ไม่มี** stack trace, ชื่อตาราง หรือข้อความจาก driver หลุดออกมา | 6.4 |
| AC-N11 | ตรวจ log ของกรณีใน AC-N10 | มีรายละเอียดพอให้ตามรอยได้ที่ฝั่ง server | 6.4 |
| AC-N12 | เปิดพนักงานคนเดียวกันในสองหน้าต่าง โหลดข้อมูลทั้งคู่ แล้วบันทึกจากหน้าต่าง A จากนั้นบันทึกจากหน้าต่าง B ที่ยังถือข้อมูลชุดเก่าอยู่ | B ได้ `200` ไม่มี `409` และไม่มีคำเตือนเรื่องการแก้ชนกัน ค่าที่อ่านได้หลังจากนั้นเป็นของ B และ `updated_at` เป็นเวลาที่ B บันทึก | 6.5, 1.2 |
| AC-N14 | เปิดหน้ารายการบนเบราว์เซอร์ที่ติดตั้งได้ในเครื่องที่ตรวจ อย่างน้อยหนึ่งตัวที่ใช้เอนจิน Chromium (Chrome หรือ Edge) และหนึ่งตัวที่ใช้ Gecko (Firefox) | ใช้งานได้ที่ความกว้างหน้าจอเดสก์ท็อปทั่วไปโดยไม่ต้องเลื่อนแนวนอน และได้ผลเหมือนกันทั้งสองเอนจิน | 6.8 |
| AC-N15 | `[DB]` รัน `EXPLAIN` ของ query เบื้องหลัง `GET /api/employees?department_id=1&sort=salary` โดยตั้ง `enable_seqscan = off` | แผนใช้ index ตามหัวข้อ 6.1 ได้จริง แสดงว่า query ไม่ได้เขียนในรูปที่ทำให้ index ใช้ไม่ได้ เช่นครอบฟังก์ชันทับคอลัมน์ที่ทำ index ไว้ | 6.1 |
| AC-N16 | `[DB]` ดู query ที่เกิดขึ้นจริงจากการเรียก `GET /api/employees` หนึ่งครั้งบน baseline | จำนวน query ไม่เพิ่มตามจำนวนแถวในหน้า คือไม่มี N+1 จากการดึงชื่อแผนกทีละแถว และ `meta.total` มาจาก `COUNT` ใน SQL ไม่ใช่การดึงทุกแถวมานับในแอป | 6.1, 4.2 |

### 7.9 เกณฑ์ที่ยังตรวจไม่ได้ใน build นี้

สามข้อนี้เคยอยู่ในหัวข้อ 7.8 แต่รันจริงไม่ได้ จึงย้ายออกมา **ไม่นับผ่านและไม่นับไม่ผ่าน** เก็บไว้ที่นี่เพื่อไม่ให้ข้อกำหนดต้นทางหายไปเงียบ ๆ

| ID | ทำไมรันไม่ได้ | ต้องมีอะไรจึงจะรันได้ | ตรวจอะไรแทนไปก่อน |
|---|---|---|---|
| **AC-N02** p95 < 300 ms ที่ 100,000 แถว | ข้อมูลจริงมี 5 แถว และการวัด p95 ต้องยิงซ้ำหลายรอบด้วยเครื่องมือวัดที่โปรเจกต์ยังไม่มี ตัวเลข 300 ms เองก็เป็นเป้าหมายที่ตั้งขึ้นตอนเขียน spec ไม่ได้มาจากข้อกำหนดของผู้ใช้ | สคริปต์สร้างข้อมูลจำลอง เครื่องมือยิงโหลด และสภาพแวดล้อมที่ควบคุมได้ซึ่งไม่ใช่เครื่องพัฒนา | **AC-N03**, **AC-N15**, **AC-N16** — ตรวจสาเหตุของความช้าคือ index หาย, query เขียนจน index ใช้ไม่ได้ และ N+1 ได้โดยไม่ต้องมีข้อมูลจำนวนมาก |
| **AC-N13** ไม่เปิดสู่อินเทอร์เน็ตสาธารณะ | เป็นคุณสมบัติของการตั้งค่าเครือข่ายตอน deploy ไม่ใช่พฤติกรรมของแอป และ build นี้ยังไม่มีการ deploy จริง ตัวแอปเองไม่มีทางทำให้ข้อนี้ผ่านหรือไม่ผ่าน | สภาพแวดล้อมที่ deploy แล้ว พร้อมสิทธิ์ตรวจ firewall หรือ ingress | ย้ายไปเป็น **เงื่อนไขก่อนขึ้นระบบ** ในหัวข้อ 6.6 ผู้ที่ deploy เป็นผู้รับผิดชอบ ไม่ใช่ผู้ตรวจแอป |
| **AC-N14** เฉพาะส่วน Safari / WebKit | Safari ไม่มีรุ่นสำหรับ Windows ซึ่งเป็นเครื่องที่ใช้พัฒนาโปรเจกต์นี้ จึงติดตั้งเพื่อทดสอบไม่ได้เลย | เครื่อง macOS หรือบริการทดสอบข้ามเบราว์เซอร์ | **AC-N14** ส่วนที่เหลือ คือตรวจบนเอนจิน Chromium และ Gecko ซึ่งติดตั้งได้จริงบน Windows |

ถ้าภายหลังมีสภาพแวดล้อมครบ ให้ย้ายข้อเหล่านี้กลับเข้าหัวข้อ 7.8 ตามเลขเดิม เลข AC ทั้งสามสงวนไว้ ไม่นำไปใช้กับเกณฑ์อื่น

### 7.10 เกณฑ์ตัดสินว่าเสร็จ

ถือว่าแอปผ่านการตรวจรับเมื่อ

1. **เกณฑ์ P0 ทั้ง 39 ข้อตามตารางในหัวข้อ 7.0.1 ผ่านทั้งหมด** — นี่คือเงื่อนไขเดียวที่บล็อกการส่งมอบ
2. ในจำนวนนั้น กับดักเฉพาะของชุดข้อมูลนี้ต้องผ่านครบ ได้แก่ **AC-I05** (`"In Active"` ต้องได้ `false`), **AC-I03** (วันที่ไม่เลื่อน), **AC-I02/AC-V05** (เงินเดือนไม่ถูกตัดที่คอมมา) และ **AC-I07** (`id` ถัดไปคือ `106`)
3. เกณฑ์ P1 ที่ไม่ผ่าน **ถูกบันทึกไว้พร้อมเลข AC** ไม่ใช่ปล่อยผ่านเงียบ ๆ แต่ไม่บล็อกการส่งมอบ
4. ไม่มีความสามารถใดในหัวข้อ 1.2 หลุดเข้ามาในระบบ โดยเฉพาะช่องอัปโหลดไฟล์และปุ่ม export

ข้อแนะนำเพิ่มเติม ไม่ใช่เงื่อนไขบล็อก — ถ้าจะรัน import ซ้ำในสภาพแวดล้อมจริง ควรให้ **AC-I08** (รันซ้ำแล้วไม่มีอะไรเปลี่ยน) ผ่านก่อน มิฉะนั้น `updated_at` ของทุกแถวจะขยับทุกครั้งที่ seed

ข้อที่ไม่ผ่านให้บันทึกเลข AC กำกับไว้ เพื่ออ้างกลับไปยังหัวข้อต้นทางใน spec ได้ทันที
