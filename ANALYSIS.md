# Analysis — exam_data.xlsx

เอกสารสรุปสิ่งที่พบจากไฟล์ `example_data/exam_data.xlsx` และข้อสรุปการออกแบบฐานข้อมูลกับกฎการ import ที่ตกลงกันแล้ว

ขอบเขตของไฟล์ต้นทาง: 2 ชีต คือ `Example Data` (7 คอลัมน์ × 5 แถว) และ `Requirement` (นิยาม UI ของแต่ละคอลัมน์)

---

## 1. ข้อค้นพบสำคัญจากไฟล์

ตรวจจาก XML ภายในไฟล์ `.xlsx` โดยตรง ไม่ได้ดูจากหน้าจอ Excel เพราะสิ่งที่ตาเห็นกับสิ่งที่เก็บจริงไม่ตรงกัน

| คอลัมน์ | ค่าที่เก็บจริง | ชนิดจริง | รูปแบบที่ใช้แสดงผล |
|---|---|---|---|
| ID | `101`–`105` | number | — |
| Name | shared string | text | — |
| Department | shared string | text | — |
| Salary | `65000`, `58000`, … | **number** | `numFmtId=4` → `#,##0.00` |
| Join Date | `44941`, `45007`, … | **serial จำนวนเต็ม** | `numFmtId=188` → `[$-409]d-mmm-yy;@` |
| Status | `"Active"` / `"In Active"` | text | — |
| Last Updated Date | `46032`, `45996`, … | **serial จำนวนเต็ม** | `numFmtId=188` |

**ข้อค้นพบที่ 1 — คอมมาและรูปแบบวันที่เป็นเพียงการแสดงผล**
`65,000.00` ที่เห็นบนหน้าจอ แท้จริงคือเลข `65000` และ `15-Jan-23` คือเลข `44941`
ผลคือปัญหาคลาสสิกอย่าง `parseFloat("65,000.00")` ได้ `65` จะไม่เกิดขึ้น **ตราบใดที่อ่านไฟล์ `.xlsx` โดยตรง** แต่จะกลับมาทันทีถ้ามีขั้นตอนแปลงเป็น CSV คั่นกลาง จึงกำหนดเป็นข้อห้ามของระบบ

**ข้อค้นพบที่ 2 — ความเสี่ยงจริงของวันที่คือ timezone ไม่ใช่การ parse**
เมื่อค่าเป็น serial ความเสี่ยงย้ายไปอยู่ที่ขั้นตอนแปลง หาก library แปลง serial เป็น `Date` ตาม timezone ของเครื่อง แล้วนำไปเก็บเป็น UTC วันที่จะ **เลื่อนไป 1 วัน** โดยไม่มี error ใด ๆ ซึ่งอันตรายกว่าปัญหาการ parse สตริงเพราะตรวจไม่พบ

**ข้อค้นพบที่ 3 — ไฟล์ไม่มีข้อมูลเวลาเลย**
serial ทุกตัวเป็นจำนวนเต็ม ไม่มีเศษทศนิยม แปลว่ามีแต่วันที่ ไม่มีเวลาในวัน มีผลโดยตรงต่อการกำหนด `updated_at`

**ข้อค้นพบที่ 4 — ไฟล์ไม่มี `dataValidation` เลยแม้แต่จุดเดียว**
"Dropdown List" และ "Checkbox" ใน `Requirement` เป็นข้อกำหนดของ **หน้าจอที่จะสร้าง** ไม่ใช่ข้อจำกัดที่บังคับอยู่ในไฟล์ ช่อง Department และ Status ในไฟล์เป็นข้อความอิสระที่พิมพ์อะไรก็ได้ จึงไม่สามารถไว้ใจค่าจากไฟล์ได้ และต้อง validate ทุกแถวเสมอ

**ข้อค้นพบที่ 5 — เซลล์ว่างจะหายไปทั้ง element**
ใน XML เซลล์ที่ไม่มีค่าจะไม่มีแท็ก `<c>` ปรากฏเลย ตัวอ่านที่ไล่ตามลำดับที่พบจะทำให้คอลัมน์ **เลื่อนทั้งแถว** ต้องอ่านโดยอ้าง cell reference (`A2`, `B2`) เสมอ

---

## 2. Schema ที่สรุปแล้ว

| คอลัมน์ใน Excel | คอลัมน์ใน DB | ชนิด | UI control |
|---|---|---|---|
| ID | `id` | `INT` identity, PK | read-only |
| Name | `name` | `VARCHAR(255) NOT NULL` | text input |
| Department | `department_id` | `INT NOT NULL` FK → `departments` | dropdown |
| Salary | `salary` | `DECIMAL(12,2) NOT NULL` | number input |
| Join Date | `join_date` | `DATE NOT NULL` | date picker |
| Status | `is_active` | `BOOLEAN NOT NULL DEFAULT true` | checkbox |
| Last Updated Date | `updated_at` | `TIMESTAMPTZ NOT NULL` | read-only |

```sql
CREATE TABLE departments (
  id   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR(100) NOT NULL UNIQUE
);

CREATE TABLE employees (
  id            INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name          VARCHAR(255)  NOT NULL,
  department_id INT           NOT NULL REFERENCES departments(id),
  salary        DECIMAL(12,2) NOT NULL,
  join_date     DATE          NOT NULL,
  is_active     BOOLEAN       NOT NULL DEFAULT true,
  updated_at    TIMESTAMPTZ   NOT NULL
);
```

เหตุผลของการตัดสินใจแต่ละข้อ:

**`Department` แยกเป็นตาราง ไม่ใช่ enum** — เปิดทางให้เพิ่มแผนกใหม่ได้โดยไม่ต้อง migrate schema และ dropdown ต้องมี endpoint ดึงข้อมูลอยู่แล้ว การมีตารางจึงได้ทั้งสองอย่างจากที่เดียว

**`Salary` เป็น `DECIMAL(12,2)` ไม่ใช่ float** — ทศนิยม 2 ตำแหน่งหมายถึงเงินจริง float ทำให้เศษสตางค์คลาดเคลื่อนเมื่อข้อมูลสะสมมากขึ้น ส่วนรูปแบบ `#,##0.00` เป็นหน้าที่ของ FE ทั้งหมด DB ไม่เกี่ยวข้อง

**`Status` เป็น `BOOLEAN` ไม่ใช่ enum** — พิจารณา enum แล้วตัดออก เพราะ `Requirement` ระบุ checkbox ซึ่งเป็นค่าสองสถานะโดยนิยาม และเมื่อ import mapper ต้องมีอยู่แล้วไม่ว่าทางใด enum ก็ไม่ได้เพิ่มความปลอดภัยด้านชนิดข้อมูลจริง ประโยชน์ที่แท้จริงของ enum คือการขยายในอนาคต ซึ่งถ้าโดเมนเป็นสองสถานะก็ไม่จำเป็นต้องจ่ายล่วงหน้า ผลพลอยได้คือคำสะกด `"In Active"` จากไฟล์ต้นทางจะไม่เข้าไปอยู่ใน DB หรือ API contract เลย

**`updated_at` เป็น `TIMESTAMPTZ` ไม่ใช่ `TIMESTAMP`** — `TIMESTAMP WITHOUT TIME ZONE` ที่ตกลงกันเองว่าเก็บ UTC อาศัยวินัยของผู้เขียนโค้ดล้วน ๆ พอมีคนเผลอเขียนเวลา local ลงไปครั้งเดียวก็ปนกันโดยมองไม่ออก `TIMESTAMPTZ` บังคับให้ทุกค่าระบุ offset และเก็บเป็น UTC ภายใน

**`join_date` ยังเป็น `DATE` ธรรมดา** — เป็นวันที่ที่ผู้ใช้เลือกเอง ไม่มีความหมายเชิงเวลาในวัน การใส่ timezone จะสร้างโอกาสเลื่อนวันโดยไม่ได้ประโยชน์

---

## 3. กฎการ import

### 3.1 การแปลงค่า

| ค่า | วิธีแปลง |
|---|---|
| serial → วันที่ | `1899-12-30 + serial วัน` แล้วส่งต่อเป็นสตริง `YYYY-MM-DD` **ห้ามผ่าน `Date` ที่ผูก timezone** |
| กันบั๊กปี 1900 | reject serial ที่น้อยกว่า `61` |
| `join_date` | เก็บเป็น `DATE` ตรง ๆ |
| `updated_at` | เที่ยงคืน **UTC** ของวันนั้น (`YYYY-MM-DD 00:00:00+00`) เพราะไฟล์ไม่มีเวลา |
| `salary` | อ่านเป็น number จาก `.xlsx` โดยตรง **ห้ามมีขั้นตอน CSV คั่นกลาง** |

### 3.2 การ normalize ค่าข้อความ

ทั้ง Status และ Department ใช้ตรรกะเดียวกัน คือ **ตัดช่องว่างหัวท้าย → ลบช่องว่างภายใน → แปลงเป็นตัวพิมพ์เล็ก** แล้วจึงเทียบ

| ค่าที่ normalize แล้ว | ผลลัพธ์ |
|---|---|
| `active` | `is_active = true` |
| `inactive` (ครอบคลุม `"In Active"` หลังลบช่องว่าง) | `is_active = false` |
| อย่างอื่นทั้งหมด | **reject แถวนั้น** |

Department ใช้หลักเดียวกัน คือค่าที่เทียบแล้วไม่ตรงกับแถวใดใน `departments` ให้ reject ไม่สร้างแผนกใหม่อัตโนมัติ เพื่อไม่ให้คำที่พิมพ์ผิดกลายเป็นแผนกใหม่

### 3.3 นโยบายแถวเสีย

แถวที่ reject จะถูก **ข้าม ไม่ล้มทั้งไฟล์** แถวที่เหลือยัง commit ตามปกติ และต้อง log อย่างน้อย เลขแถวในชีต, `ID`, และเหตุผลสั้น ๆ เพื่อให้ย้อนกลับไปแก้ที่ไฟล์ต้นทางได้

### 3.4 การ upsert

upsert โดยใช้ `id` จากไฟล์เป็น conflict target

- **แถวใหม่** — ใช้ค่าจากไฟล์ทั้งหมด รวม `updated_at`
- **แถวที่มีอยู่แล้ว** — ไฟล์ชนะทุกคอลัมน์ **ยกเว้น `updated_at` ที่ให้ค่าเดิมใน DB ชนะ** วิธีทำคือไม่ใส่ `updated_at` ไว้ใน `DO UPDATE SET`

```sql
INSERT INTO employees (id, name, department_id, salary, join_date, is_active, updated_at)
VALUES (...)
ON CONFLICT (id) DO UPDATE SET
  name          = EXCLUDED.name,
  department_id = EXCLUDED.department_id,
  salary        = EXCLUDED.salary,
  join_date     = EXCLUDED.join_date,
  is_active     = EXCLUDED.is_active;
  -- updated_at เจตนาไม่อยู่ในรายการนี้ ค่าเดิมจึงถูกรักษาไว้
```

> **ข้อควรรู้** กฎนี้กัน `updated_at` ไม่ให้เดินถอยหลังได้สำเร็จ แต่แลกมาด้วยการที่แถวซึ่งถูกไฟล์แก้ค่าจริง เช่น เงินเดือนเปลี่ยน จะยังแสดง `updated_at` เป็นวันเดิม คือ stamp ไม่สะท้อนการเปลี่ยนแปลงที่มาจากการ import
> ทางเลือกที่ได้ทั้งสองอย่างคือ `updated_at = GREATEST(employees.updated_at, EXCLUDED.updated_at)` ซึ่งยังกันการถอยหลัง แต่รับค่าที่ใหม่กว่าจากไฟล์ได้ — **ยังไม่ได้ตัดสินใจ**

### 3.5 การรีเซ็ต identity sequence

ไฟล์มี `id` เริ่มที่ `101` หาก import ค่าเหล่านี้เข้าไปโดยไม่ทำอะไรต่อ sequence เบื้องหลังจะยังอยู่ที่ `1` และจะชนกันเมื่อไต่ขึ้นมาถึงช่วง `101`–`105` จึงต้องรีเซ็ตให้เลย `max(id)`

```sql
SELECT setval(
  pg_get_serial_sequence('employees', 'id'),
  (SELECT MAX(id) FROM employees)
);
```

สองข้อที่ต้องระวัง

1. ต้องใช้ `GENERATED BY DEFAULT AS IDENTITY` ไม่ใช่ `GENERATED ALWAYS` มิฉะนั้นจะ insert `id` ที่ระบุเองไม่ได้
2. ต้องรัน **ทุกครั้งหลัง import** ไม่ใช่เฉพาะครั้งแรก เพราะ upsert รอบถัดไปอาจพา `id` ที่สูงขึ้นเข้ามาอีก

ผลลัพธ์ตามที่ต้องการคือ พนักงานที่สร้างใหม่ผ่านระบบคนถัดไปจะได้ `id` = `106`

---

## 4. สรุปปัญหาและวิธีแก้

| # | ปัญหา | วิธีแก้ |
|---|---|---|
| 1 | Status เป็นข้อความอิสระ และ `"In Active"` สะกดแยกคำ ทำให้การตรวจแบบตรงไปตรงมาให้ผลกลับด้าน | normalize แล้วเทียบกับตาราง alias ค่าที่ไม่รู้จัก reject รายแถว ห้ามใส่ค่า default |
| 2 | serial + timezone ทำให้วันที่เลื่อน 1 วันโดยไม่มี error | แปลง serial เองจาก epoch `1899-12-30` ส่งต่อเป็นสตริง `YYYY-MM-DD` ไม่ผ่าน `Date` |
| 3 | ไฟล์ไม่มีเวลา แต่ `updated_at` เป็น timestamp | กำหนดเป็นเที่ยงคืน UTC ของวันนั้น และใช้ `TIMESTAMPTZ` เพื่อบังคับเจตนานี้ |
| 4 | auto-stamp ของ ORM จะทับ `updated_at` จากไฟล์ ซึ่งต้องรักษาไว้ | ไม่ใช้ auto-update column ในเส้นทาง import เปิดให้เซ็ตค่าได้เฉพาะทรานแซกชันนั้น ส่วน API ปกติยัง stamp เองตามเดิม |
| 5 | Department ไม่มี master list | แยกตาราง `departments` seed จาก DISTINCT ในไฟล์ แผนกที่ไม่รู้จัก reject ไม่สร้างอัตโนมัติ |
| 6 | `id` เริ่มที่ `101` ทำให้ sequence ชนภายหลัง | `setval` ให้เลย `max(id)` หลัง import ทุกครั้ง และใช้ `GENERATED BY DEFAULT` |
| 7 | import ซ้ำแล้วข้อมูลซ้ำ | upsert บน `id` ไฟล์ชนะทุกคอลัมน์ ยกเว้น `updated_at` |
| 8 | เซลล์ว่างหายไปทั้ง element ทำให้คอลัมน์เลื่อน | อ่านโดยอ้าง cell reference เสมอ และ reject แถวที่มีเซลล์ว่างเพราะทุกคอลัมน์เป็น NOT NULL |
| 9 | หัวคอลัมน์มีช่องว่าง (`Last Updated Date`) | map เป็น snake_case แบบระบุชัดในโค้ด ไม่ใช้การสร้างคอลัมน์อัตโนมัติจากหัวตาราง |
| 10 | คอมมาใน Salary (เกิดเฉพาะเมื่อผ่าน CSV) | อ่าน `.xlsx` โดยตรงเสมอ ห้ามมีขั้นตอน CSV คั่นกลาง |

---

## 5. ข้อแลกเปลี่ยนที่รับไว้อย่างรู้ตัว

- **`updated_at` ไม่สะท้อนการแก้ที่มาจากการ import** — ดูหัวข้อ 3.4 เลือกกันการถอยหลังเป็นหลัก
- **ทุกคอลัมน์เป็น `NOT NULL`** — ไฟล์ตัวอย่างไม่มีคอลัมน์ optional จึงยึดตามนั้น ไม่เพิ่ม field ที่โจทย์ไม่ได้ขอ
- **ไม่มีคอลัมน์สกุลเงิน และไม่มีวันที่เริ่ม inactive** — อยู่นอกขอบเขตที่ตกลงไว้
- **checkbox เป็น UI ที่ผูกกับสองสถานะ** — วันที่ต้องการสถานะที่สาม ทั้ง schema และ UI ต้องเปลี่ยนพร้อมกัน

---

## ภาคผนวก — ข้อมูลตัวอย่างหลังแปลงค่า

| id | name | department | salary | join_date | is_active | updated_at (UTC) |
|---|---|---|---|---|---|---|
| 101 | John Doe | Engineering | 65000.00 | 2023-01-15 | `true` | 2026-01-10 00:00:00+00 |
| 102 | Jane Smith | Marketing | 58000.00 | 2023-03-22 | `true` | 2025-12-05 00:00:00+00 |
| 103 | Alice Wong | Sales | 45000.00 | 2024-06-01 | `true` | 2026-02-14 00:00:00+00 |
| 104 | Bob Brown | Engineering | 72000.00 | 2022-11-10 | `false` | 2026-03-01 00:00:00+00 |
| 105 | Charlie Day | HR | 50000.00 | 2024-02-19 | `true` | 2026-04-20 00:00:00+00 |

`departments` ที่ seed ได้จากไฟล์: `Engineering`, `Marketing`, `Sales`, `HR`
